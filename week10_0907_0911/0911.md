# CI/CD

## 1. CI/CD

### 1) 일반적인 개발 워크플로

- 로컬에서 단위 테스트 실행
    - 중앙 레포지토리에서 최신 코드를 로컬로 가져온 후 요구 사항을 구현
    - 요구사항을 구현할 때 TDD(테스트 주도 개발)를 많이 사용
    - 구현된 코드는 단위 테스트를 통과할 때까지 반복적으로 수정

- 중앙 레포지토리로 코드 푸시 및 병합

- 병합 후 코드 컴파일
    - 구현을 완료한 코드가 메인 브랜치에 병합되면 코드 컴파일을 수행
    - 새로 병합된 코드로 인해 기존에 없던 컴파일 오류가 발생할 수 있음

- 컴파일된 코드에서 테스트 실행
    - 단위 테스트와 통합 테스트를 실행해 회귀 테스트를 수행해서 결함이 있는지를 확인
    - 정적 분석(코딩 표준 준수 및 불필요한 코드의 존재 여부를 확인하는 과정) 같은 작업을 수행하기도 함

- 아티팩트 배포
    - 병합된 코드의 단계별 품질 점검을 끝내면 모든 코드를 패키징하고 최종 사용자가 사용할 수 있도록 서버에 배포
    - 아티팩트는 일반적인 하나의 압축된 파일

### 2) CI/CD 개요

- 기본 개념은 지속적인 통합(Continuous Integration), 지속적인 서비스 제공(Continuous Delivery), 지속적인 배포(Continuous Deployment)

- 애플리케이션 워크플로를 자동화해서 애플리케이션을 보다 짧은 주기로 고객에게 제공하는 방법

- 자동화해서 개발 및 운영 팀에서 발생하는 문제(Integration Hell) 를 해결하기 위한 방법

- 지속적인 서비스 제공이 지속적인 배포를 포함하기도 한다. 
- CI/CD 는 파이프라인으로 표현되는 실제 프로세스를 의미하고 애플리케이션 개발에 지속적인 자동화 및 지속적인 모니터링을 추가하는 것을 의미

### 3) CI - 지속적인 통합

- 개발 팀이 작은 변경 사항을 구현하고 코드를 버전 제어 레포지토리에 자주 체크인하도록 하는 코딩 철학이자 일련의 관행
- 폴리 글랏의 문제: 애플리케이션은 다양한 플랫폼과 도구에서 코드를 개발해야 하기 때문에 팀은 작은 변경 사항을 통합하고 검증하기 위한 매커니즘이 필요

- 기술 목표는 애플리케이션을 빌드, 패키지, 테스트 하나는 일관되고 자동화된 방법을 설정하는 것

- 통합 프로세스의 일관성을 유지하면 팀이 코드 변경을 더 자주 커밋할 가능성이 높아져 공동 작업과 소프트웨어 품질이 향상

- 모든 개발을 완료한 후에 품질 관리를 적용하는 고전적인 방법을 대체하는 방법으로 소프트웨어의 질적 향상과 소프트웨어를 배포하는데 걸리는 시간을 줄이는데 초점을 맞춤

### 4) CD

- **지속적인 서비스 제공(Continuous Delivery)**
    - 개발자들이 애플리케이션에 적용한 변경 사항이 버그 테스트를 거쳐 레포지토리(이미지 저장소 또는 컨테이너 레지스트리)에 자동으로 업로드하는 것

    - 운영팀은 이 레포지토리에서 애플리케이션을 실시간 프로덕션 환경으로 배포
    - 최소한의 노력으로 새로운 코드를 배포하는 것

- **지속적인 배포(Continuous Deployment)**
    - 변경 사항을 레포지토리에서 고객이 사용 가능한 프로덕션 환경까지 자동으로 릴리스하는 과정
    - 애플리케이션 제공 속도를 저해하는 수동 프로세스로 인한 운영 팀의 프로세스 과부하 문제를 해결

### 5) 지속적 인도

- 전통적 인도 서비스의 문제점
    - 느린 인도 기간
    - 느린 피드백 주기
    - 자동화 미비
    - 위험한 핫픽스: 간단한 테스트만 수행하거나 테스트 생략
    - 스트레스
    - 의사 소통 부족
    - 책임의 분산
    - 낮은 업무 만족도

- 지속적 인도
    - 장점
        - 빠른 제품 인도
        - 빠른 피드백 주기
        - 위험도가 낮은 릴리스
        - 유연한 릴리스 가능

### 6) 자동 배포 파이프라인

코드 변경 → 지속적 통합 → 자동 인수 테스트 → 구성 관리 변경 → 배포

### 7) 컨테이너 인프라 환경에서의 CI/CD

- 개발자가 소스를 commit 하고 push 하면 CI 단계로 진입
- CI 단계에서는 애플리케이션이 자동 빌드되고 테스트를 거쳐 배포할 수 있는 애플리케이션인지 확인
- 테스트를 통과하면 신뢰할 수 있는 애플리케이션으로 간주하고 CD 단계로 넘김
- CD 단계에서는 애플리케이션을 컨테이너 이미지로 만들고 파드, 디플로이먼트, 스테이트 풀셋 등 다양한 오브젝트 조건에 맞춰 미리 설정한 파일을 통해 배포

- 도구
    - TeamCity: JetBrain에서 kotlin을 기반으로 만든 도구인데 Kotlin DSL 이라는 스크립트 언어로 작업을 구성
    - Git Hub Action
        - Github에서 지원하는 워크플로 기반의 CI/CD 도구
        - Github에 저장한 소스 코드를 자동 분석한 결과를 기반으로 Github 액션이 추천하는 방식이나 개발자가 직접 워크플로를 정의하는 파일을 작성한 후 Github 저장소에 넣어 사용
        - 무료는 한달에 2000분이라는 제한이 있음
    - Bamboo
        - Atlassian에서 만든 CI/CD 도구로 유료로 사용자의 서버에 설치해서 사용
        - Atlassion에서 만든 Jira, Confluence, Trello, Bitbucket 등과 연동이 쉬움
    - GitLab CICD
        - GitLab에서 제공하는 강력한 DevOps 도구로 프로젝트 내에서 코드 변경 사항을 자동으로 테스트, 빌드, 배포할 수 있도록 지원
    - Jenkins
        - 사용자가 직접 UI에서 작업을 구성하거나 작업 순서를 코드로 정의할 수 있음
        - 오랜 시간 동안 많은 사람들이 사용하고 있어서 사용에 필요한 정보를 찾기가 쉽고 플로그래인 개발 관련 커뮤니티 활동이 활발해서 다양한 사용 환견, 언어 및 빌드 도구와 연계할 플러그인이 필요한 경우 쉽게 찾을 수 있음
        - 특정 언어나 환경에 구애받지 않고 범용적인 목적으로 무난하게 사용할 수 있음
    - public cloud 환경에서는 자체적으로 배포가 가능한 CI/CD 도구(AWS CodeBuild, CodePipeline, Code Deploy 등)를 사용하기도 하고 배포가 중요한 환경에서는 CD 기능에 중점을 둔 Spinnaker, Argo CD를 선택적으로 도입할 수 있음

- 구축
    - SCM: github, gitlab, bitbucket
    - CI: Jenkins, Circle Ci를 이용해서 자동화 스크립트 작성
    - 배포 환경 선택: docker & Kubernetes
    - Ansible, Argo CD: 프로비저닝 과 구성 관리 및 애플리케이션을 자동화하는 도구

## 2. Git Hub Action

### 1) 개요

- End To End Git Hub 중심 소프트웨어 개발 생명주기 프로세스
- 소프트웨어 개발에 필요한 작업 주기를 자동화하는 도구
- 이벤트 기반으로 동작하는데 특정 이벤트가 발생했을 때 Workflow가 Trigger 되고 Workflow가 지정하는 Runner라는 시스템에서 Job을 자동으로 실행시키는 형태
- 요금은 레포지토리가 public 이거나 액션을 실행하는 시스템이 직접 호스팅 경우는 무료

- Starter Workflow
    - 예시를 제공: https://github.com/actions/starter-workflows
    - 액션을 공유: https://github.com/marketplace?type=actions

### 2) 구성 요소

- Event: 정의된 Git Hub Action 작업을 실행시키는 특정 활동
    - 외부 트리거(Git Hub 외부에서 발생한 사건)의 발동
    - Git Hub 저장소에서 무언가를 수행(push, merge, pr 등)
    - 특정 시간 혹은 간격마다 수행
    - 사용자가 직접 워크플로를 실행

- Step
    - Git Hub Action의 최소 실행 단위
    - uses 절을 통해 가져오고 run 절에 의해서 실행

- Runner
    - 워크플로 코드가 실행되는 물리/가상 서버 및 컨테이너
    - 기본적으로는 Git Hub이 제공하는 시스템을 러너로 사용

- Job
    - 스텝을 모아두고 어떤 러너에서 실행할 지 정의하는 컴포넌트
    - 전체 워크플로의 중하위 목료를 완수하게끔 설계
    - CI/CD 파이프라인의 경우 빌드, 테스트, 패키징을 각각 하나의 잡으로 구성
    - 잡의 실행 결과를 깃허브 액션 인터페이스를 통해서 확인하지만 성공 혹은 실패는 잡 단위로 표기될 뿐 스텝 어디에서 이상이 있는지는 나오지 않으므로 각 잡을 만들 대 어느 스텝에서 에러가 났는지 파악할려면 깊게 들어가지 않고도 확인이 가능한 편한 크기로 만들기를 권장

### 3) 작성 방법

- 저장소에서 Actions 탭에서 직접 생성
- 코드 내부의 `.github/workflows` 디렉토리 안에 yaml 파일을 만드는 것

### 4) 기본 구조

```yaml
name: 이름
on:
이벤트
jobs:
작업 나열
```

- name: 해당 워크플로의 이름

- on: 이벤트를 작성
    - 코드 변경 이벤트
        - push
        - pull_request
        - pull_request_target
        - create/delete: 브랜치 또는 태그를 만들거나 삭제할 때

```yaml
on:
  push:
    branches: ["main", "dev"]
  pull_request:
    types: [opened, reopened]
```

    - 수동 및 외부 호출 이벤트
        - workflow_dispatch: 직접 실행 경우
        - repository_dispatch: 외부 앱이나 서버에서 GitHub API를 호출해서 워크플로를 깨울 때
        - workflow_call: 다른 워크플로에서 이 워크플로를 재사용하는 경우

    - 스케쥴링

```yaml
on:
  schedule:
    - cron: '분 시 ...'
```

    - GitHub 활동 관련 이벤트
        - issue
        - issue_comment
        - release
        - watch

- jobs
    - 실행할 작업을 정의
    - 하나의 작업 내의 명령들은 같은 환경에서 실행
    - runs-on: 잡이 실행되는 환경으로 linux, mac os, windows를 지원
    - steps: 실제 동작을 정의
        - name: 이름
        - uses: 작업을 가져와서 사용하는 경
        - run: 명령어 입력

```yaml
jobs:
  runs-on: ubuntu-latest
  steps:
  - name: Install tree
    run: |
      sudo apt update
      sudo apt install tree
```

### 5) Git Hub Action 테스트

- 원격 레포지토리 생성: githubaction-sample
- 로컬에 디렉토리 생성하고 git 초기화: `git init`
- 파일 하나를 만들고 원격 저장소에 push

```bash
git add .
git commit -m "git hub action sample"
git remote add origin https://github.com/itstudy001/githubaction-sample.git
git push -u origin main
```

- 프로젝트에 `.github/workflows` 디렉토리를 생성하고 yaml 파일을 작성

```yaml
name: CI Example

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository Code
        uses: actions/checkout@v4
      - name: One-Line Print
        run: echo 'Hello Git Hub Action'
      - name: Set Up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - name: Display Python version
        run: python --version
```

- 코드 푸시

### 6) node를 실행

- 원격 레포지토리 생성: nodetest

- 노드 프로젝트 생성
    - 디렉토리를 생성
    - 디렉토리 안에서 npm init(명령어가 없다고 나오면 nodejs 설치)
    - 필요한 패키지 설치: `npm install mocha`
    - 디렉토리에 파일을 추가: `test.spec.js`

node에서 테스트를 수행할 때 사용하는 파일

```javascript
describe('Default Test Set', () => {
    it('test1 should be passed',() => {
        console.log('test1 passed')
    })
    
    it('test2 should be passed',() => {
        console.log('test2 passed')
    })

})
```

- 실행: `./node_modules/.bin/mocha test.spec.js`

- 명령어를 간단하게 만들기: package.json의 scripts에 추가

```json
"scripts": {
    "test": "./node_modules/.bin/mocha test.spec.js"
  },
```

- `npm test`를 수행

- 원격 레포지토리에 푸시
    - `.gitignore`를 만들어서 node_modules를 푸시에서 제외하기
    - git 초기화: `git init`
    - 스테이지 영역으로 이동: `git add .`
    - 로컬 레포지토리로 이동: `git commit -m "nodejs push"`
    - 원격 레포지토리 연결: `git remote add origin https://github.com/itstudy001/nodetest.git`
    - 푸시: `git push -u origin main`

- git hub에서 push가 발생할 때 마다 테스트를 수행하는 action을 생성: `.github/workflows/nodetest.yaml`

```yaml
name: Node.js CI
on:
  push:
    branches: [ main ]
jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [20.x]

    steps:
      - uses: actions/checkout@v4 #레포지토리에서 코드를 가져오기

      - name: Set UP Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}

      - run: npm install
      - run: npm run build --if-present
      - run: npm test
```

- 코드를 푸시

### 7) 환경 변수

- 개요
    - 환경 변수를 정의해서 사용하는 것이 가능

- 기본 환경 변수
    - git hub 나 runner에서 제공하는 환경 변수
    - GITHUB_ 또는 RUNNER로 시작
    - https://docs.github.com/en/actions/reference/workflows-and-actions/variables

- Secret 이나 Variable
    - 비밀번호는 코드에 직접 삽입하면 안됨
    - Git Hub에서 Secret으로 만들고 git hub action에서 빌드를 할 때 비밀번호를 삽입하는 것이 가능합니다.

### 8) Python 테스트 프로젝트를 Docker Hub에 이미지로 배포

- 데이터베이스로 사용할 MongoDB 컨테이너를 생성: `docker run --name mongodb -v ~/data:/data/db -d -p 27017:27017 mongo`

- 프로젝트 생성
    - 프로젝트를 위한 디렉토리 생성
    - 가상환경 생성 및 활성화

```bash
python -m venv .venv
.venv\Scripts\activate 또는 source .venv/bin/activate
```

    - 패키지 설치: flask, requests, beautifulsoup4, pymongo, pytest
    - 프로젝트에 `app.py` 파일을 추가하고 작성

```python
from flask import Flask, render_template, jsonify, request

app = Flask(__name__)

from pymongo import MongoClient

client = MongoClient('mongo', 27017)
db = client.dbsparta

@app.route('/')
def home():
    return render_template('index.html')

if __name__ == '__main__':
    app.run('0.0.0.0', port=5000, debug=True)
```

    - 실행: `python app.py`
    - 프로젝트에 templates 디렉토리를 생성하고 index.html 파일을 추가해서 작성

```html
<!DOCTYPE html>
<html>
    <head>
        <title>index.html</title>
    </head>
    <body>
        <h3>Flask Web Application</h3>
    </body>
</html>
```

    - 프로젝트에 `utils.py` 파일을 만들고 함수를 작성

```python
def getTitle():
    return "title"
```

    - 테스트를 수행할 파일을 만들고 함수를 작성(test_util.py)

```python
from utils import getTitle

def test_getTitle():
    title = getTitle()
    assert title == "title"
```

    - 테스트 수행: `pytest`

- git hub action을 이용해서 테스트
    - 패키지 정보를 내보내기: `pip list --format=freeze > requirements.txt`
    - 프로젝트에 `.github/workflows` 디렉토리를 생성하고 yaml 파일을 추가하고 작성: `ci-test.yaml`

```yaml
name: ci-test
on:
  push:
    branches:
      - main
jobs:
  run-test-code:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - run: pip install -r requirements.txt
      - run: pytest
```

    - 코드를 원격 레포지토리에 push
    - actions 탭에서 성공 여부를 확인
