# Kubernetes

## 9. Packaging

### 1) kustomize

- **개요**
    - 쿠버네티스 매니페스트 관리 도구로 매니페스트의 구성 관리를 구현
    - kustomize 의 기본인 매니페스트 생성 기능은 kubectl 도 제공
    - base 매니페스트의 변경 위치를 정의한 패치 매니페스트를 준비하고 병합된 매니페스트를 출력

- **제공하는 기능**
    - 여러 매니페스트 파일을 하나의 매니페스트로 병합
    - 레이블 등 공통 부분을 설정하여 매니페스트 파일의 중복 감소
    - 오버레이의 재사용성을 통해 복수의 환경 구성 관리가 용이
    - 시크릿 생성

- **설치**
    - kubectl 에도 kustomize 가 있지만 kustomize 의 다양한 기능을 이용하고자 할 때는 별도로 설치
    - `asdf`
        - 여러 언어와 도구의 버전을 한 곳에서 관리할 수 있는 통합 버전 관리자
        - 프로그래밍 언어마다 별도의 버전 관리 도구가 존재하는데 `asdf` 는 이러한 도구들을 하나로 통합하여 하나의 설정 파일을 모든 도구의 버전을 제어
        - **설치**

            ```bash
            sudo apt update
            sudo apt install curl git
            git clone https://github.com/asdf-vm/asdf.git ~/.asdf --branch v0.14.0
            echo '. "$HOME/.asdf/asdf.sh"' >> ~/.bashrc
            source ~/.bashrc
            asdf version
            asdf reshim
            ```
    - **asdf 를 이용해서 kustomize 설치**
        - `asdf plugin add kustomize`
        - `asdf install kustomize 5.3.0`
        - `asdf global kustomize 5.3.0`

- **기본적인 사용**
    - 작업 디렉토리 생성: `mkdir -p echo/bash`
    - 작업 디렉토리로 이동: `cd echo/bash`
    - Deployment 매니페스트 작성: `deployment.yaml`

        ```yaml
        apiVersion: apps/v1
        kind: Deployment
        metadata:
          name: echo
          labels:
            app.kubernetes.io/name: echo
        spec:
          replicas: 1
          selector:
            matchLabels:
              app.kubernetes.io/name: echo
          template:
            metadata:
              labels:
                app.kubernetes.io/name: echo
            spec:
              containers:
              - name: nginx
                image: ghcr.io/jpubdocker/simple-nginx-proxy:v0.1.0
                env:
                - name: NGINX_PORT
                  value: "80"
                - name: SERVER_NAME
                  value: "localhost"
                - name: BACKEND_HOST
                  value: "localhost:8080"
                - name: BACKEND_MAX_FAILS
                  value: "3"
                - name: BACKEND_FAIL_TIMEOUT
                  value: "10s"
                ports:
                - name: http
                  containerPort: 80
              - name: echo
                image: ghcr.io/jpubdocker/echo:v0.1.0
        ```
    - 서비스 매니페스트: `service.yaml`

        ```yaml
        apiVersion: v1
        kind: Service
        metadata:
          name: echo
          labels:
            app.kubernetes.io/name: echo
        spec:
          selector:
            app.kubernetes.io/name: echo
          ports:
          - name: echo
            port: 80
            targetPort: http
            protocol: TCP
        ```
    - 인그레스 매니페스트: `ingress.yaml`

        ```yaml
        apiVersion: networking.k8s.io/v1
        kind: Ingress
        metadata:
          name: echo
          labels:
            app.kubernetes.io/name: echo
        spec:
          ingressClassName: nginx
          rules:
          - host: echo.jpub.local
            http:
              paths:
              - pathType: Prefix
                path: /
                backend:
                  service:
                    name: echo
                    port:
                      number: 80
        ```
    - **매니페스트 정리**
        - `kustomization.yaml` 파일을 생성해서 매니페스트 파일을 결합하는 것이 kustomize 의 기본 기능
        - `kustomize create --autodetect` 명령을 사용하면 현재 디렉토리 내의 매니페스트 파일을 정리하기 위한 `kustomization.yaml` 파일을 생성할 수 있음
        - kustomization.yaml 파일의 내용

            ```bash
            apiVersion: kustomize.config.k8s.io/v1/beta1
            kind: Kustomization
            resources:
            - deployment.yaml
            - ingress.yaml
            - service.yaml
            ```
        - resources 항목에 구성 관리의 대상이 되는 매니페스트 파일이 상대 경로로 나오게 됨
        - 정리를 한 결과를 출력하고자 할 때는 `kustomize build .`
        - 배포: `kustomize build . | kubectl apply -k .`
        - 삭제: `kustomize build . | kubectl delete -k .`

- **공통 부분 설정**
    - 쿠버네티스에서 다양한 매니페스트를 생성하면 metadata.labels 과 같이 매니페스트에 나타나는 몇 가지 메타 데이터가 있는데 이런 부분을 간단하게 관리 가능
    - `deployment.yaml` 파일에 `label` 부분을 주석 처리

        ```yaml
        apiVersion: apps/v1
        kind: Deployment
        metadata:
          name: echo
        #  labels:
        #    app.kubernetes.io/name: echo
        spec:
          replicas: 1
        #  selector:
        #    matchLabels:
        #      app.kubernetes.io/name: echo
          template:
            metadata:
        #      labels:
        #        app.kubernetes.io/name: echo
            spec:
              containers:
              - name: nginx
                image: ghcr.io/jpubdocker/simple-nginx-proxy:v0.1.0
                env:
                - name: NGINX_PORT
                  value: "80"
                - name: SERVER_NAME
                  value: "localhost"
                - name: BACKEND_HOST
                  value: "localhost:8080"
                - name: BACKEND_MAX_FAILS
                  value: "3"
                - name: BACKEND_FAIL_TIMEOUT
                  value: "10s"
                ports:
                - name: http
                  containerPort: 80
              - name: echo
                image: ghcr.io/jpubdocker/echo:v0.1.0
        ```
    - `service.yaml` 파일의 `label` 도 주석 처리

        ```yaml
        apiVersion: v1
        kind: Service
        metadata:
          name: echo
        #  labels:
        #    app.kubernetes.io/name: echo
        spec:
        #  selector:
        #    app.kubernetes.io/name: echo
          ports:
          - name: echo
            port: 80
            targetPort: http
            protocol: TCP
        ```
    - `ingress.yaml` 파일의 `label` 도 추석 처리

        ```yaml
        apiVersion: networking.k8s.io/v1
        kind: Ingress
        metadata:
          name: echo
        #  labels:
        #    app.kubernetes.io/name: echo
        spec:
          ingressClassName: nginx
          rules:
          - host: echo.jpub.local
            http:
              paths:
              - pathType: Prefix
                path: /
                backend:
                  service:
                    name: echo
                    port:
                      number: 80
        ```
    - 레이블 설정: `kustomize edit set label "app.kubernetes.io/name:echo"`

    - 확인: `cat kustomization.yaml`
        - commonLabel 항목의 모든 매니페스트에 적용되는 레이블을 설정

            ```bash                          
            apiVersion: kustomize.config.k8s.io/v1beta1
            kind: Kustomization
            resources:
            - deployment.yaml
            - ingress.yaml
            - service.yaml
            commonLabels:
              app.kubernetes.io/name: echo
            ```
    - 빌드 결과 출력: `kustomize build .`

- **재사용과 부분 오버레이**
    - dev 환경을 가정하고 이 환경에 적합한 매니페스트 구성 관리를 진행
    - kustomize 는 base 디렉토리에 리소스를 위치시키고 overlays 디렉토리에 설정 파일을 위치시키는 것이 일반적
    - 여러 환경(개발 환경과 운영 환경 등)에 배포해야 하는 경우 유용한 기능
    - base 디렉토리의 내용을 재사용해서 각 환경에 필요에 따라 설정을 오버레이 할 수 있음
    - echo 디렉토리에서 `overlays/dev` 디렉토리를 생성
        - `mkdir -p overlays/dev`
    - `overlays/dev` 디렉토리에 `kustomization.yaml` 파일 생성

    - 리소스 복사: `dev` 디렉토리에서 수행: `kustomize create --resources ../../base`

    - overlays/dev 디렉토리에 kustomization.yaml 파일 확인
        ```yaml
        apiVersion: kustomize.config.k8s.io/v1beta1
        kind: Kustomization
        resources:
        - ../../base
        ```
    - kustomize 에는 매니페스트 파일을 그대로 패치 파일로 다루고 쿠버네티스의 최적의 형태로 병합해주는 `Strategic Merge Patch` 구조가 존재
    - 디플로이먼트의 패치 파일 생성: `patch-deployment.yaml`

        ```yaml
        apiVersion: apps/v1
        kind: Deployment
        metadata:
          name: echo
          labels:
            app.kubernetes.io/name: echo
        spec:
          replicas: 2
          selector:
            matchLabels:
              app.kubernetes.io/name: echo
          template:
            metadata:
              labels:
                app.kubernetes.io/name: echo
            spec:
              containers:
              - name: nginx
                image: ghcr.io/jpubdocker/simple-nginx-proxy:v0.1.0
                env:
                - name: NGINX_PORT
                  value: "80"
                - name: SERVER_NAME
                  value: "localhost"
                - name: BACKEND_HOST
                  value: "localhost:8080"
                - name: BACKEND_MAX_FAILS
                  value: "5"
                - name: BACKEND_FAIL_TIMEOUT
                  value: "10s"
                ports:
                - name: http
                  containerPort: 80
              - name: echo
                image: ghcr.io/jpubdocker/echo:v0.1.0
        ```

    - 원하는 데이터만 수정: `patch-ingress.yaml`

        ```yaml
        - op: replace
          path: "/spec/rules/0/host/"
          value: "dev-echo.jpub.local"
        ```
    - 패치 파일 추가 명령
        - `kustomize edit add patch --patch patch-deployment.yaml`
        - `kustomize edit add patch --kind Ingress --path patch-ingress.yaml --name echo`
    - 이 기능을 이용해서 매니페스트의 중복을 줄이고 효율적인 구성 관리를 할 수 있음
    - 버전 관리도 kustomize build 를 해서 나온 결과를 관리하는 것이 아니라 변경된 내용을 가진 디렉토리 단위로 관리

- **Secret 사용**
    - 완성된 매니페스트를 출력할 때 시크릿의 매니페스트를 생성
    - 시크릿의 매니페스트는 버전 관리 대상에서 제외할 수 있음
    - 외부에서 secret 을 만들어서 kustomize 에서 주입하는 방식

- **네트워크를 경유해서 매니페스트 생성**
    - 매니페스트 파일과 같이 특정 리소스에 대한 `kustomization.yaml` 이 공개되어 있으면 이 파일만 있으면 바로 배포할 수 있음
    - Ingress Nginx Controller 의 kustomization 파일을 확인
        - `curl https://raw.githubusercontent.com/kubernetes/ingress-nginx/controller-v1.8.1/deploy/static/provider/cloud/kustomization.yaml`

### 2) Helm

- **개요**
    - CNCF 가 관리하는 쿠버네티스 패키지 관리 도구
    - 오픈 소스
        - `Helm` > `Chart` > `Manifest`

- **Helm, Chart, Manifest 의 관계**
    - 여러 매니페스트 파일의 템플릿을 정리할 수 있는데 대규모 애플리케이션이라면 매니페스트 템플릿도 많아지고 관리도 복잡하기 때문에 `Chart` 로 분할해서 관리
    - `Chart` 는 다른 `Chart` 와 의존 관계를 가질 수 있음
    - 서드 파티의 `Helm` 차트 리포지토리에서 다운로드 가능
    - `Helm` 으로 `Chart` 를 관리하고 `Chart` 를 이용해서 매니페스트를 관리

- **Helm 설치**
    - **직접 설치**

    ```bash
    sudo apt-get install curl gpg apt-transport-https --yes
    
    curl -fsSL https://packages.buildkite.com/helm-linux/helm-debian/gpgkey | gpg --dearmor | sudo tee /usr/share/keyrings/helm.gpg > /dev/null
    
    echo "deb [signed-by=/usr/share/keyrings/helm.gpg] 
    https://packages.buildkite.com/helm-linux/helm-debian/any/ any main" | sudo tee 
    /etc/apt/sources.list.d/helm-stable-debian.list
    
    sudo apt-get update
    
    sudo apt-get install helm
    ```
    - **asdf 로 설치**
        - `asdf plugin add helm`
        - `asdf install helm 3.13.3`
        - `asdf global helm 3.13.3`
    - 설치 확인: `helm version`

- **Helm Chart 와 Repository**
    - 자주 사용하는 Repository
        - `stable`: Helm 공식 stable 버전 Chart Repository 인데 현재 개발은 멈춘 상태
        - `incubator`: 공식 stable 버전의 이전 버전으로 현재 개발은 멈춘 상태
        - `bitnami`: bitnami 에서 운영하는 서드 파티 repository 로 현재도 개발 진행 중
    - 레포지토리 확인: `helm repo list`
    - 레포지토리 추가: `helm repo add bitnami https://charts.bitnami.com/bitnami`
    - 차트 검색: `helm search repo 이름`
        - 헬름 허브에서 검색: `helm search hub 이름`
        - 브라우저에서 검색: `https://artifacthub.io`

- **차트를 이용한 설치**
    - 설치

        ```bash
        helm install [릴리스명][Chart 레포지토리 이름] [차트이름]
        --namespace [설치할 네임스페이스]
        --create-namespace
        --versoin [차트버전]
        --values [커스텀 values 파일 경로]
        ```
    - nginx 헬름을 조회: `helm search repo bitnami/nginx`

    - 설정 가능한 파라미터 확인: `helm show values bitnami/nginx`
    - `helm install my-nginx bitnami/nginx`
    - 설치 확인: `helm ls`
    - 파드 배포 확인: `kubectl get pods,svc`
    - 삭제: `helm uninstall my-nginx`

- **MariaDB 설치**
    - 조회: `helm search repo bitnami/mariadb -l`
    - 파라미터 확인: `helm show values bitnami/mariadb`
    - 설치: `helm install my-mariadb bitnami/mariadb`
    - 설치 확인
        - `helm ls`
        - `kubectl get pods`: 파드가 pending 상태
        - 로그나 파드의 상세보기를 통해서 에러를 확인: pvc 연결 오류
    - 삭제: `helm uninstall my-mariadb`

    - 파라미터를 이용해서 스토리지 사용하지 않는 형태로 생성
        - `helm install my-mariadb bitnami/mariadb --set primary.persistence.enabled=false --set auth.rootPassword=1234`
    - 파드를 확인: `kubectl get pods`
    - 파드 내부 컨테이너에 접속: `kubectl exec -it my-mariadb-0 -- bash`
    - mariadb 삭제: `helm uninstall my-mariadb`

- **PV 와 PVC 를 생성해서 mariadb 에 연결해서 설치**
    - PV 와 PVC 생성
        - 디렉토리 생성: `sudo mkdir -p /data/mariadb`
        - 소유자 변경: `sudo chown -R 1001:1001 /data/mariadb`
        - 권한 변경: `sudo chmod 777 /data/mariadb`

        - PV 매니페스트 생성: `mariadb-pv.yaml`
            ```yaml
            apiVersion: v1
            kind: PersistentVolume
            metadata:
              name: mariadb-hostpath-pv
              labels:
                type: local
            spec:
              storageClassName: manual
              capacity:
                storage: 1Gi
              accessModes:
                - ReadWriteOnce
              hostPath:
                path: /data/mariadb
                type: DirectoryOrCreate
            ```
        - PV 배포: `kubectl apply -f mariadb-pv.yaml`
        - mariadb values 설정을 위한 values 파일: `mariadb-values.yaml`

            ```yaml
            auth:
              rootPassword: "SecretRootPassword123!"
              database: "testdb"
              username: "dbuser"
              password: "UserPassword123!"

            primary:
              persistence:
                enabled: true
                existingClaim: "mariadb-pvc"

            architecture: standalone
            ```
        - 배포: `helm install my-mariadb bitnami/mariadb -f mariadb-values.yaml`
        - 삭제: `helm install my-mariadb`
            - 이 때 pv, pvc는 삭제가 안돼서 따로 삭제해야함.

- **helm 을 직접 생성**
    - 기본 구조
        - `Chart.yaml`: 메타 데이터 설정, 기본 정보 설정
        - `values.yaml`: 실제 채워질 값들에 대한 설정
        - `templates/`: 리소스들이 위치하는 디렉토리(deployment, service, pv, pvc, secret, ingress 등)
    - 디렉토리 생성 및 이동: `custom-mariadb`
    - `Chart.yaml` 파일 작성

        ```yaml
        apiVersion: v2
        name: custom-mariadb
        description: A custom Helm Chart
        type: application
        version: 0.1.0
        appVersion: "11.4"
        ```
    - 값을 채우는 `values.yaml`

        ```yaml
        image:
          repository: mariadb
          tag: "11.4"
          pullPolicy: IfNotPresent

        mariadb:
          rootPassword: "SecretRootPassword123!"
          database: "appdb"
          user: "appuser"
          password: "UserPassword123!"

        persistence:
          hostPath: "/data/mariadb"
          storageSize: "1Gi"
          nodeName: ""

        service:
          type: ClusterIP
          port: 3306
        ```

    - secret 파일: `template/secret.yaml`

- 디렉토리 구조를 자동으로 생성해서 값만 변경해서 사용하는 것도 가능: `helm create 이름`


