# CI/CD

## Jenkins

### 9) SSH를 이용한 배포

- SSH 로 배포할 서버 작업
    - SSH 접속이 가능한 서버를 1개 준비

    - 자바를 설치: 자신의 프로젝트에서 사용한 버전과 같거나 높은 버전 설치
        - `sudo apt install openjdk-21-jdk`

    - 소스 코드를 복사할 디렉토리를 생성: `mkdir ~/deploy`

- Jenkins 작업
    - Jenkins 에서 SSH 를 사용하기 위해서 플러그인 설치: publish Over SSH
    - SSH 접속 키를 생성
        - Jenkins 가 설치된 컴퓨터에서 키를 생성: `ssh-keygen -t rsa -b 4096`
        - Docker 로 설치했으므로 Docker 컨테이너 내부에서 작업: `docker exec -it jenkins bash`

    - 키 값 복사 (pub)
    - 접속하려는 컴퓨터의 `~/.ssh/authorized_keys` 에 추가

### 10) 주기적인 빌드

=> Cron Job

- 일정한 주기를 가지고 작업
- 운영체제의 시간 기반 잡 스케쥴

=> Project > Configure > Triggers

- Build Periodically: 변경내용이 없어도 주기적으로 빌드를 수행하는 cron job
- Poll SCM: 변경 내역이 있는 경우에만 수행하는 Cron Job

### 11) Web Hook

=> Git Hub에 변경사항이 생기면 Jenkins에게 알려주고 빌드를 하는 방식

=> GitHub에 Access Token을 발급 받아야 하는데 workflow 권한과 admin:repo_hook 권한이 있어야 합니다.

`<GITHUB_ACCESS_TOKEN>`

=> Jenkins에 GitHub에 로그인 하기 위한 Credential을 생성

- Secret Text를 선택한 후 내용을 설정
- Secret에 token 값을 설정

=> jenkins 에 GitHub Server를 등록: Jenkins관리 -> Sysetm에서 등록

=> Jenkins 프로젝트에서 Trigger에서 GitHub hook trigger for GITScm polling 체크

=> GitHub 의 프로젝트의 settings 탭에서 Webhook 생성

`http://Jenkins가설치된컴퓨터의IP:포트번호/프로젝트이름/` 을 Payload URL에 설정해야 합니다.

=> 이 예제는 Jenkins가 외부에서 접속할 수 있는 환경에 설치되어 있어야 합니다.

### 12) 도커 허브에 이미지 배포

=> 스프링 프로젝트의 Dockerfile을 만들 때는 Dockerfile 파일 안에서 컴파일이나 빌드를 할 것인지 아니면 외부에서 빌드를 해서 jar 나 war 파일을 가져올 것인지 결정

```dockerfile
FROM amazoncorretto:21
CMD ["./mvnw", "clean", "package"]
COPY ./build/libs/Calculator-0.0.1-SNAPSHOT.jar app.jar
ENTRYPOINT ["java", "-jar", "app.jar"]
```

=> Docker Hub에서 Access Token 발급

`<DOCKER_HUB_ACCESS_TOKEN>`

=> DockerHub에 Repository 생성: calculator

=> Item의 execute shell 부분 수정

```sh
chmod +x ./gradlew
./gradlew compileJava
./gradlew clean build

echo "Logging into Docker Hub"
DOCKER_USERNAME="ggnagpae1"
DOCKER_PASSWORD="<DOCKER_HUB_ACCESS_TOKEN>"
echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
echo "Logging into Docker Hub"
docker build -t ggnagpae1/calculator .
docker push ggnagpae1/calculator
```

=> 빌드를 하면 도커에서 젠킨스를 사용하거나 현재 운영체제에 도커가 없으면 에러가 발생

=> 컨테이너에 접속해서 도커를 설치

- `docker exec -it -u root jenkins /bin/bash`

```sh
apt update

apt install ca-certificates
apt install curl
apt install gnupg
apt install lsb-release

mkdir -p /etc/apt/keyrings

#linux 버전에 맞게 설치
curl -fsSL https://download.docker.com/linux/debian/gpg | gpg --dearmor -o /etc/apt/keyrings/docker.gpg

echo \
  "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/debian \
  $(lsb_release -cs) stable" | tee /etc/apt/sources.list.d/docker.list > /dev/null

## - Install Docker Engine
apt-get update

apt-get install docker-ce docker-ce-cli containerd.io docker-compose-plugin
```

=> 접속 권한 변경

```sh
chmod 666 /var/run/docker.sock
```

=> USERNAME 과 PASSWORD를 secret으로 변경

- 구성에서 Environment 섹션에서 수행
- Use secret text(s) or file(s)를 누그고 Binding +Add를 클릭
- Username and password (separated)를 클릭하고 변수 이름 과 값을 설정

- 코드 변경

```sh
DOCKER_USERNAME=$DOCKERHUB_USER
DOCKER_PASSWORD=$DOCKERHUB_PASSWORD
```

### 11) pipeline

=> 개요

- 연속적인 이벤트 혹은 Job의 그룹
- 순차적 혹은 병렬적으로 실행시키거나 특별하게 작성한 스크립트로 이벤트들을 연속적으로 실행시키는 등의 일을 지원하는 기능
- jenkins pipeline은 Jenkins를 사용해서 연속적인 전달 파이프라인의 통합 및 구현을 지원하는 플러그인의 집합
- 파이프라인은 파이프라인 DSL을 통해 간단하거나 복잡한 전달 파이프라인을 코드로 생성할 수 있는 확장 가능한 자동화 서버를 갖추고 있음

=> 빌드 후 조치를 이용한 다른 프로젝트 빌드

- 프로젝트에서 [빌드 후 조치 추가]를 선택한 후 Build other projects 메뉴가 있는데 이를 선택하면 빌드가 끝난 후 다른 프로젝트를 실행하는 것이 가능

=> Pipeline 시각화 작업

- 플로그 인 설치: Delivery Pipeline
- 프로젝트 보기 화면에서 All 옆의 + 버튼을 눌러서 새로운 뷰를 생성

=> 작성 방법

- Declarative
- Scripted(Groovy + DSL(도메인 특화 언어 - 도메인에만 적용되는 특별한 언어))

=> 작성 방법의 차이점

- 특정 stage 실행 가능 여부: Declarative 방식은 작업이 실패하면 다음 작업을 수행하지 못하지만 Scripted 방식은 다음 작업 수행이 가능
- Scripted 방식은 제어문 사용 가능
- Scripted 방식은 Pipeline으로 시작하고 Declarative 방식은 node로 시작

=> 파이프라인 프로젝트 생성

- Declarative 방식으로 스크립트를 작성

```groovy
node{
    stage("Ready"){
        sh "echo 'Ready'"
    }
    stage("Build"){
        sh "echo 'Build Jar'"
    }
    stage('Deploy'){
        sh "echo 'Deploy AWS'"
    }
}
```

- 빌드
- 확인
- 중간 빌드를 실패하면 다음 작업은 빌드되지 않습니다

=> 파이프라인 프로젝트 수정

- Scripted 방식으로 스크립트를 작성

```groovy
pipeline{
    agent any
    stages{
        stage('Compile'){
            steps{
                echo 'Complile Successfully'
            }
        }
        stage('Junit'){
            steps{
                echo 'Junit Passed Successfully'
            }
        }
        stage('Code Analysis'){
            steps{
                echo 'Code Analysis Completed Successfully'
            }
        }
        stage('Deploy'){
            steps{
                echo 'Deployed Successfully'
            }
        }
    }
}
```

- 빌드 코드를 추가

```groovy
pipeline{
    agent any
    stages{
        stage('Compile'){
            steps{
                echo 'Complile Successfully'
            }
        }
        stage('Junit'){
            steps{
                echo 'Junit Passed Successfully'
            }
        }
        stage('Code Analysis'){
            steps{
                echo 'Code Analysis Completed Successfully'
            }
        }
        stage('Deploy'){
            steps{
                echo 'Deployed Successfully'
            }
        }
    }
    post{
        always{
            echo 'This will always run'
        }
        success{
            echo 'This will run when finished successfully'
        }
        failure{
            echo 'This will run if failed'
        }
        unstable{
            echo 'This will run when the run way marked as unstable'
        }
        changed{
            echo 'This will run when the state of the pipeline has changed'
        }
    }
}
```

=> git clone 명령

```groovy
git branch: '브랜치이름', url:'git url'
```

=> Jenkins의 스크립트 파일을 git에 두고 수행

- git hub에 레포지토리를 만들고 스크립트 파일(Jenkinsfile)을 생성하고 작성

```groovy
pipeline{
    agent any
    stages{
        stage('Compile'){
            steps{
                echo 'Compile Successfully'
            }
        }
        stage('Junit'){
            steps{
                echo 'JUnit test Successfully'
            }
        }
    }
}
```

### 12) 원격 서버에 도커 이미지로 애플리케이션 배포

=> 이전에 만든 프로젝트를 가져오기: git clone

```groovy
pipeline{
    agent any
    stages{
        stage('github clone'){
            steps{
                git branch:'main', url:'https://github.com/itstudy001/calculator.git'
            }
        }
    }
}
```

=> 빌드 스테이지 추가

```groovy
pipeline{
    agent any
    stages{
        stage('github clone'){
            steps{
                git branch:'main', url:'https://github.com/itstudy001/calculator.git'
            }
        }
         stage('permission'){
            steps{
                sh "chmod +x ./gradlew"
            }
        }
        stage('Build'){
            steps{
                sh "./gradlew clean build --stacktrace"
            }
        }
    }
}
```

=> 도커 이미지 만들기

- Dockerfile을 프로젝트에 추가해서 push

```dockerfile
FROM amazoncorretto:21
CMD ["./mvnw", "clean", "package"]
COPY ./build/libs/Calculator-0.0.1-SNAPSHOT.jar app.jar
ENTRYPOINT ["java", "-jar", "app.jar"]
```

- 빌드 스테이지에 이미지 빌드 코드를 추가

```groovy
stage('Image Build'){
    steps{
        sh "docker build -t ggnagpae1/calculator ."
    }
}
```

=> docker hub에 Image Push

- docker hub에 repository 생성

`ggnagpae1/calculator`

- docker hub의 access token 만들기

`<DOCKER_HUB_ACCESS_TOKEN>`

- Credential 생성(username and password로 생성)
- pasword 에 token 값을 설정하고 username에 계정을 설정하고 ID는 구별하기 위한 이름을 설정

- Script 변경

```groovy
pipeline{
    agent any
    environment{
        DOCKER_CREDENTIALS_ID = 'dockerhub'
    }
    stages{
        stage('github clone'){
            steps{
                git branch:'main', url:'https://github.com/itstudy001/calculator.git'
            }
        }
         stage('permission'){
            steps{
                sh "chmod +x ./gradlew"
            }
        }
        stage('Build'){
            steps{
                sh "./gradlew clean build --stacktrace"
            }
        }
        stage('Image Build'){
            steps{
                sh "docker build -t ggnagpae1/calculator ."
            }
        }
        stage('Docker Login'){
            steps{
                script{
                    withCredentials([usernamePassword(credentialsId:env.DOCKER_CREDENTIALS_ID, usernameVariable: 'DOCKER_USERNAME', passwordVariable:'DOCKER_PASSWORD')]){
                        sh 'echo $DOCKER_PASSWORD | docker login -u $DOCKER_USERNAME --password-stdin'
                    }
                }
            }
        }
    }
}
```

=> Docker가 설치된 SSH 서버를 준비

- jenkins가 설치된 곳에서 `ssh-keygen -t rsa -b 4096` 명령어로 private 키 와 public key를 생성
- jenkins에는 Publish Over SSH 와 SSH Agent 플러그인을 설치
- jenkins에 SSH username with private key를 선택해서 ID, username, private key를 설정합니다.
- Passphrase 옵션은 키를 생성할 때 비밀번호를 설정했으면 입력하고 없으면 비워둠

=> Git Repository에 script.sh 파일을 만들어서 리눅스 서버에서 도커로 도커 허브의 이미지를 다운로드 받아서 컨테이너로 생성하는 코드를 작성

```sh
IMAGE_NAME="도커허브의이미지경로"
TAG="latest"

#기존 이미지가 있는지 확인
if docker images | grep -q "$IMAGE_NAME"; then
  echo "이미지 $IMAGE_NAME 삭제 중..."
  docker rmi -f "$IMAGE_NAME:$TAG"
fi

#컨테이너도 삭제
docker ps -q -f name=calculator | grep -q . && sudo docker stop calculator

#새로운 이미지 다운로드
echo "이미지 $IMAGE_NAME:$TAG 다운로드 중..."
docker pull "$IMAGE_NAME:$TAG"

docker run -d -p 외부포트:내부포트 --rm --name calculator "$IMAGE_NAME:$TAG"
docker image prune -f
echo "작업 완료!!!"
```

=> stage 추가

```groovy
stage('ssh'){
    steps{
        echo 'SSH'
        sshagent(credentials: ['credentials ID']){
            sh "ssh -p 포트번호 -o StrictHostKeyChecking=no 계정@IP 'git clone https://깃헙URL'"
            sh "ssh -p 포트번호 -o StrictHostKeyChecking=no 계정@IP 'chmod 777 디렉토리/script.sh'"
            sh "ssh -p 포트번호 -o StrictHostKeyChecking=no 계정@IP '디렉토리/script.sh'"
            sh "ssh -p 포트번호 -o StrictHostKeyChecking=no 계정@IP 'rm -rf 디렉토리'"
```
