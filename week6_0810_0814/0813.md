# Micro Service

프로젝트

- https://github.com/itggangpae/cqrs_write.git
- https://github.com/itggangpae/cqrs_read.git

## 2. MySQL, MongoDB, Spring Boot, Kafka를 이용한 CQRS 구현

### 4) Kafka 연결

- post 방식으로 설정
- URL은 http://localhost:8080/cqrs/book
- headers 부분에 Content-Type key를 추가하고 application/json 을 설정
- Body 부분에 raw 에 json으로 변경한 후 입력
    ```json
    {
        "title":"오딧세이",
        "author":"호메로스",
        "category":"신화",
        "description":"크리스토퍼 놀란",
        "pages":356,
        "price":15000,
        "published_date":"2026-08-13"
    }
    ```
- send를 누르고 데이터베이스 확인

### 5) Kafka 연결

- DataWrite 프로젝트 수정(cqrs_write)
    - 라이브러리 설정(프로젝트를 생성할 때 수행했음)
        - built.gradle 파일의 dependencies 
        - `implementation 'org.springframework.boot:spring-boot-starter-kafka'` 추가 후 코끼리 모양의 아이콘을 눌러서 rebuild

    - 카프카 설정: application.yaml 파일
        ```yaml
        spring:
          kafka:
            bootstrap-servers: 127.0.0.1:9092
            consumer:
              group-id: adamsoft
              auto-offset-reset: earliest
              key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
              value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
            producer:
              key-serializer: org.apache.kafka.common.serialization.StringSerializer
              value-serializer: org.apache.kafka.common.serialization.StringSerializer
        ```

    - 카프카 환경 설정 클래스 추가: KafkaConfiguration
        ```java
        @Configuration
        public class KafkaConfiguration {
            @Value("${spring.kafka.bootstrap-servers}")
            private String bootStrapServers;

            @Bean
            public ProducerFactory<String, String> producerFactory(){
                Map<String, Object> configs = new HashMap<>();
                configs.put(ProducerConfig.BOOTSTRAP_SERVERS_CONFIG, bootStrapServers);
                configs.put(ProducerConfig.KEY_SERIALIZER_CLASS_CONFIG, StringSerializer.class);
                configs.put(ProducerConfig.VALUE_SERIALIZER_CLASS_CONFIG, StringSerializer.class);
                return new DefaultKafkaProducerFactory<>(configs);
            }

            @Bean
            public KafkaTemplate<String, String> kafkaTemplate(){
                return new KafkaTemplate<>(producerFactory());
            }
        }
        ```

    - BookDTO 클래스 수정
        - `bid`, `title`, `author`, `category`, `pages`, `price`, `published_date`, `description` 필드 보유

    - 카프카 메시지 전송 클래스 작성 - KafkaProducer
        ```java
        @Service
        @RequiredArgsConstructor
        public class KafkaProducer {
            private static final String TOPIC = "cqrs-topic";

            @Autowired
            private KafkaTemplate<String, String> kafkaTemplate;
            private final Logger log = LoggerFactory.getLogger(getClass());

            public void sendMessage(BookDTO bookDTO){
                String message = "{\"bid\":" + "\"" + bookDTO.getBid() + "\"}";
                //메시지 전송
                this.kafkaTemplate.send(TOPIC, message);
            }
        }
        ```

- Service 클래스 수정
    - BookService에서 책 정보를 저장한 뒤, 쓰기 작업이 완료되면 KafkaProducer를 통해 카프카에 메시지(bid) 전송
        ```java
        @Service
        @RequiredArgsConstructor
        public class BookService {
            private final BookRepository bookRepository;
            private final KafkaProducer kafkaProducer;

            public void saveBook(BookDTO bookDTO){
                try {
                    SimpleDateFormat formatter = new SimpleDateFormat(
                            "yyyy-MM-dd", Locale.ENGLISH);
                    Date published_date = formatter.parse(bookDTO.getPublished_date());
                    Book book = Book.builder()
                            .title(bookDTO.getTitle())
                            .author(bookDTO.getAuthor())
                            .category(bookDTO.getCategory())
                            .pages(bookDTO.getPages())
                            .price(bookDTO.getPrice())
                            .published_date(published_date)
                            .description(bookDTO.getDescription())
                            .build();
                    bookRepository.save(book);
                    bookDTO.setBid(book.getBid());
                    //쓰기 작업을 완료할 때 카프카에게 메시지를 전송
                    kafkaProducer.sendMessage(bookDTO);
                }
                catch(Exception e){
                    System.out.println(e.getMessage());
                }
            }
        }
        ```

- 터미널을 열어서 cqrs_topic을 확인할 수 있는 consumer 를 생성
    - docker exec -it kafka /bin/bash
    - kafka-topics.sh --create --zookeeper zookeeper:2181 --replication-factor 1 --partitions 1 --topic cqrs-topic
    - kafka-topics.sh --bootstrap-server localhost:9092 --list
    - kafka-console-consumer.sh --topic cqrs-topic --bootstrap-server localhost:9092 --from-beginning

- DataRead 프로젝트 수정(cqrs_read)
    - build.gradle 파일에 의존성 추가
        - `implementation 'org.json:json:20190722'`

    - 카프카 설정: application.yaml 파일 (DataWrite와 동일하게 설정)

    - 카프카 환경 설정 클래스 추가: KafkaConfiguration (DataWrite와 동일한 코드)

    - Consumer 클래스 생성 - kafkaConsumer
        ```java
        @Service
        public class kafkaConsumer {
            @KafkaListener(topics="cqrs-topic", groupId = "adamsoft")
            public void consumer(String message) throws IOException{
                System.out.println("message:" + message);
                //JSON 파싱: JSON문자열로 온 것을 자바 객체로 변환
                JSONObject messageObj = new JSONObject(message);

                //MongoDB 컬렉션에 연결
                MongoClient mongoClient = MongoClients.create("mongodb://localhost:27017");
                MongoDatabase database = mongoClient.getDatabase("mymongo");
                MongoCollection<Document> mongo_books =
                        database.getCollection("books");
                //받은 데이터로 삽입할 데이터를 생성
                Document book = new Document();
                book.append("bid", messageObj.getLong("bid"));
                mongo_books.insertOne(book);
                mongoClient.close();
            }
        }
        ```

- 실행 시킴


## 3. 클라우드 보안

### 1) 클라우드 보안 사고

- S3 버킷을 public 으로 설정해서 고객 정보 유출된 사례

- **개발자가 AWS Access 키를 github에 올림**
    - 최근에는 github에서 AWS Access key를 올리면 push 가 안되도록 설정을 해두었고 AWS 에서는 Github에서 직접 Key를 가지고 접근하면 계정을 잠금
- **과도한 권한 설정**

### 2) 클라우드 보안이 중요한 이유
- 비용
- 신뢰
- 법

### 3) IAM(Identity and Access Management)

- **주요 구성 요소**
    - User
    - Group
    - Role(역할): 권한의 묶음
    - Policy(정책): 권한

- **Root 사용자와 IAM 사용자**

    | 구분 | 루트 사용자 | IAM 사용자 |
    |----|-----|-----|
    | 권한 | 모든 권한 소유 | 부여된 권한만 사용 | 
    | 생성 | 자동 생성 | 루트 사용자가 생성 |
    | 삭제 | 안됨 | 가능 |
    | 일상 사용 | 사용하지 않음 | 일상 업무에 사용 |

- **루트 계정에서 수행**
    - AWS 계정 이름, 이메일, 비밀번호 변경
    - 결제 정보 변경
    - AWS Support 플랜 변경
    - 계정 해지
    - IAM 사용자의 권한 복원

- **Role**
    - 사용 사례
        - AWS 서비스 간 접근: EC2가 S3에 접근하고자 하는 경우 EC2에 역할을 부여
        - 다른 AWS 계정에서 접근하고자 하는 경우
        - 임시 접근 권한
- **Policy**
    - 무엇을 허용하고 무엇을 거부할지 적어놓은 규칙표
    - JSON으로 직접 작성 가능하고 GUI를 이용해서 작성 가능
    - **기본 구조**
        - `Effect`: 허용 또는 거부에 관련된 것으로 Allow & Deny
        - `Action`: 무엇을 수행할 것인지를 정하는 것으로 `"s3:GetObject"`
        - `Resource`: 자원인데 `"arn:aws:s3::my-bucket/*"`
    - **예시**
        ```json
        {
            "Version": "2012-10-17"
            "Statement": [
                {
                    "Effect": "Allow",
                    "Action": [
                        "s3:GetObject",
                        "s3:ListBucket"
                    ],
                    "Resource": [
                        "arn:aws:s3::my-bucket"
                    ]
                }
            ]
        }
        ```
    - 정책을 만들 때 allow 와 deny 가 동시 적용되면 `deny`
- 최소 권한 정책
- 루트 계정은 반드시 `MFA(다단계 인증)`를 설정

- **IAM 모범 사례**
    - 루트 계정에 MFA를 설정: 마스터 키가 도난당하면 전체가 위험
    - IAM 계정 생성: 누가 어떤 작업을 했는지 알 수 있기 때문
    - 그룹으로 권한 관리: 일관되고 효율적인 권한 관리
    - 최소 권한 원칙: 불필요한 권한은 사고의 시작
    - 강력한 비밀번호 정책 설정: 약한 비밀번호는 쉽게 뚫림
    - 액세스 키를 코드에 넣지 말 것: 코드가 유출되면 키도 유출
    - 사용하지 않는 사용자/키 정리: 방치된 계정은 해커의 표적
    - IAM 역할 활용: 액세스 키보다 안전
- **비밀번호 정책 설정**
    - **권장 비밀번호 정책**
        - 최소 길이는 12자 이상: 짧은 비밀번호는 무차별 대입 공격에 취약
        - 대문자, 소문자, 숫자, 특수 문자 포함: 비밀번호 복잡도 증가
        - 비밀번호 만료 시간을 설정해서 주기적으로 변경하면 유출 위험 감소
        - 재사용 방지하는 것을 권장하는데 최근 12개 기억해서 이전 비밀번호 재사용을 방지한다.
        - 사용자가 변경하는 것을 허용

- **AWS 접근 방법**

| 구분 | 콘솔 로그인 | 액세스 토큰 |
|----|-----|----|
| 접근 방법 | 웹 브라우저로 AWS 콘솔 접속 | 프로그램/명령어로 AWS API를 호출 |
| 사용방법 | 아이디 + 비밀번호(+MFA) | Access Key ID + Secret Access Key |
| 사용 대상 | 사람 | 프로그램, CLI 도구, 자동화 스크립트 |
| 보안 위험 | 비밀번호 유출, 피싱 | 키를 코드에 넣으면 위험(키 대신에 IAM Role 이용을 권장) |

### 4) 네트워크 보안 아키텍쳐

- **방어 심층 전략(Defence in Depth)**

| 계층 | 서비스 | 보호 대상 | 주요 기능 |
|----|-----|----|------|
| 엣지 | Cloud Front, Shield, WAF | 외부 인터넷 경계 | DDoS 방어, L7 필터링 |
| VPC 경계 | Network, Firewall, IGW | VPC 진입점 | IDS/IPS, 상태 저장 검사 |
| 서브넷 | NACL, Route Table | 서브넷 경계 | 상태 비저장 필터링|
| 인스턴스 | Security Group | 인스턴스 경계 | 상태 저장 필터링 |
| 프로그램 | IAM, 암호화, 인증 | 애플리케이션 내부 | 인증/인가, 데이터 암호화 |

- **AWS 네트워크 서비스**
    - `AWS Shield` -> DDoS 방어가 목적, Standard(무료)는 L3/L4 자동 보호, Advanced는 L7 방어
    - `AWS WAF` -> 웹 방화벽, SQL Injection, XSS, 봇 등 L7 공격 필터링, Cloud Front, ALB에 적용
    > `SELECT * FROM member WHERE id = ? AND password = ?;`
         비밀번호에 `아무거나 or 1=1` 이면 항상 참
    - `AWS Firewall Manager` -> 중앙 관리가 목적인데 여러 계정의 WAF, Shield, SG, NACL 규칙을 중앙에서 관리
    - `AWS Network Firewall` -> VPC 방화벽으로 VPC 수준의 상태 저장/비저장 패킷 검사, IDS/IPS 기능
    - `VPC Flow Logs` -> 트래픽을 로깅하는데 VPC 내 네트워크 인터페이스의 트래픽 정보 캡쳐
    - `Traffic Mirroring` -> 패킷 미러링을 하는 것으로 트래픽을 복제해서 보안 분석 도구로 전송
    - `Route 53 Resolver DNS Firewall` -> DNS 보안을 위한 것으로 악성 도메인 DNS 쿼리 차단, DNS 터널링 방지

- **AWS WAF**
    - `AWS WAF`는 Web ACL에 규칙을 정의하고 CloudFront 또는 ALB에 연결해서 트래픽을 필터링

- **WAF 아키텍쳐**
    ```
    웹 요청(HTTP/HTTPS) -> CloudFront/ALB   -> 규칙 평가               
                        WAF Web ACL 연결      우선순위 순서대로 규칙 매칭
    
    -> Allow/Block/Count
      매칭 시 액션 실행
      미매핑 시 기본 액션
    ```

- **WAF 규칙 유형**
    - IP 기반 -> 특정 IP/CIDR 허용 또는 차단하는 형식으로 알려진 악성 IP 차단 리스트
    - Rate 기반 -> 일정 시간 내 요청 수 제한하는 것
    - 관리형 규칙 -> AWS 또는 Marketplace 제공 규칙으로 OWASP Top 10, 봇 제어

    - 사용자 정의 -> 직접 조건을 정의한 규칙으로 특정 헤더나 쿼리스트링(파라미터) 패턴 매칭
    - 정규식 패턴 -> 정규표현식 기반 매칭으로 특정 URL 패턴이나 User-Agent 필터링에 활용
    - **AWS 의 필수 관리형 규칙 그룹**
        - `AWSManagedRulesCommonRuleSet`: OWAS Top 10 공통 취약점(XSS, 파일 포함, SSRF 등)
        - `AWSManagedRulesSQLiRuleSet`: SQL Injection 공격 패턴
        - `AWSManagedRulesKnownBadInputsRuleSet`: 알려진 악성 입력(Log4j, Java Desrialization 등)
        - `AWSManagedRulesAmazonIpReputaionRuleSet`: AWS 위협 인텔리전스 기반 악성 IP
        - `AWSManagedRulesBotControlRuleSet`: 봇 트래픽 탐지 및 제어
    - **WAF 로깅 설정**
        - WAF 로그를 분석하면 공격 패턴을 파악하고 규칙을 최적화 할 수 있음

        | 로그 대상 | 장점 |
        |----|-----|
        | S3 | 대용량 저장, 비용 효율적이 되는데 Athena 로 장기 분석 |
        | CloudWatch Logs | 실시간 조회, 메트릭 필터를 이용해서 실시간 차단 현황 모니터링에 활용 |
        | Kinesis Data Firehouse | 실시간 스트리밍인데 SIEM 연동하거나 실시간 대시보드에 활용 |

- **VPC 보안 전략**
    - **VPC 보안 설계 원칙**
        - 최소 노출: 인터넷에 노출되는 리소스를 최소화하는 것으로 퍼블릭 서브넷에는 ALB/NAT 만 배치
        - 네트워크 분리: 워크로드별 네트워크 격리하는 것으로 환경(dev/stg/prod) 별 VPC 분리
        - 트래픽 제어: 허용된 트래픽만 통과시키는데 SG/NACL 화이트 리스트 방식
        - 가시성 확보: 모든 트래픽의 흐름을 기록하기 위해서 VPC Flow Logs 활성화
        - 암호화 전송: 네트워크 구간 데이터 암호화를 위해서 TLS를 강제하고 VPN/private Link 활용

    - **보안을 위한 서브넷**

    | 구분 | 서브넷 유형 | 배치 리소스 | 인터넷 접근 | 보안 수준 |
    |----|-----|-----|-----|-----|
    | 퍼블릭 티어 | 퍼블릭 서브넷 | ALB, NAT Gateway, Bastion | 인/아웃바운드 허용 | 최소 리소스 |
    | 앱 티어 | 프라이빗 서브넷 | EC2, ECS, Lambda | NAT을 통한 아웃바운드만 | ALB에서만 |
    | 데이터 티어 | 프라이빗 서브넷(격리) | RDS, ElastiCache | 인터넷 접근 불가 | 앱 티어에서만 |

    - **VPC 간 연결 보안**

    | 연결 방식 | 용도 | 보안 고려사항 |
    |----|-----|-----| 
    | VPC Peering | 두 VPC 간 프라이빗 연결 | CIDR 중복 불가, 전이적 라우팅 불가 |
    | Transit Gateway | 다수 VPC/온프레미스 허브 연결 | 라우팅 테이블로 트래픽 제어, 블랙홀 라우팅 |
    | PrivateLink | 서비스 간 프라이빗 접근 | 인터넷 노출없이 서비스 제공 |
    | Site-to-Site VPN | 온프레미스-VPC 암호화된 연결 | IPSec 터널, BGP 라우팅, 이중화 |
    | Direct Connect | 전용선 연결 | 물리적 분리, MACsec 암호화 |

    - **VPC Endpoints 보안**
        - `VPC Endpoint` 를 이용하면 인터넷 게이트웨이 없이 AWS 서비스에 프라이빗하게 접근할 수 있어 공격 표면을 줄일 수 있다.

        | 유형 | 지원 서비스 | 보안 제어 | 비용 |
        |----|------|-----|-----|
        | Gateway Endpoint | S3, Dynamo DB | 엔드포인트 정책으로 접근 제한 | 무료 |
        | Interface Endpoint | 대부분의 AWS 서비스 | Security Group 적용 가능, PrivateLink 기반 ENI | 시간당 + GB당 과금 |

        - 보안에 필수적인 VPC Endpoint
            - `S3`: 프라이빗 서브넷에서 접근할 때 인터넷 경유 방지, NAT GW 비용 절감
            - `STS`: 임시 자격 증명 발급이 프라이빗하게 이루어지도록
            - `Secrets Manager`: 비밀값 조회가 인터넷을 통하지 않도록

- `Security Group / NACL`

    | 구분 | Security Group | Network ACL |
    |----|-----|----|
    | 적용 대상 | 인스턴스 수준 | 서브넷 수준 |
    | 상태 | Stateful - 응답 자동 허용 | Stateless - 별도 규칙 필요 |
    | 기본 동작 | 인바운드 거부 / 아웃바운드 허용 | 기초 NACL: 모두 허용, 커스텀 NACL: 모두 거부 |
    | 규칙 허용 | 허용 규칙만 | 허용 및 거부 모두 가능 |
    | 규칙 평가 | 모든 규칙 종합 평가 | 번호 순서대로(첫번째 매칭에서 중단) |
    | SG 참조 | 다른 SG를 스스로 참조 가능 | IP/CIDR 만 사용 |

    - **트래픽 흐름에서의 평가 순서**
        - 인터넷 -> NACL 인바운드 -> SG 인바운드 -> 인스턴스 -> SG 자동 허용 -> NACL 아웃바운드
    - **서비스 별 Security Group 권장 설정**
        - `RDS`: 앱 SC에서만 DB 포트 허용, 퍼블릭 접근 비활성화, DB 전용 서브넷 사용
        - `ElastiCache`: 앱 SG에서만 Redis/Memcached 포트 허용, VPC 외부 접근 불가, 전송중 TLS 활성화
        - `Lambda`: 아웃바운드만 관리(인바운드 없음), Lambda SG의 아웃바운드를 필요한 서비스만 허용

### 5) 데이터 보안 전략

- **데이터 보안의 기본 원칙**
    - 기밀성
    - 무결성
    - 가용성

- **암호화 유형**

    | 구분 | 저장 데이터 암호화 | 전송 데이터 암호화 |
    |----|-----|----|
    | 보호 대상 | 디스크, S3, DB에 저장된 데이터 | 네트워크를 통해 이동하는 데이터 |
    | 방법 | AES-256 대칭 키 암호화 | TLS 1.2/1.3 프로토콜 |
    | AWS | KMS, CloudHSM | ACM(인증서 관리) |
    | 적용 대상 | EBS, S3, RDS, Dynamo DB, EFS 등 | HTTPS, VPN, PrivateLink |

- **서비스 별 암호화 설정**

    | 구분 | 자동 암호화 | 전송 암호화 | |
    |-----|-------|-----|----|
    | S3 | SSE-S3, SSE-KMS, SSE-C | HTTPS 강제(버킷 정책) | 기본 암호화 설정 가능 |
    | EBS | KMS 암호화(볼륨 생성 시) | 인스턴스-EBS간 자동 암호화 | 계정 수준 암호화 |
    | RDS | KMS 암호화(인스턴스 생성 시) | SSL/TLS 인증서 사용 | 생성 후 암호화 변경 불가
    | Dynamo DB | 기본 암호화(AWS 관리 키) | HTTPS 엔드포인트 | 고객 관리 키 선택 가능 |
    | EFS | KMS 암호화 | TLS 마운트 헬퍼 | 생성 시에만 암호화 설정 |

### 6) 보안 감사

- `AWS Config`
    - 리소스의 구성 변경을 지속적으로 기록하고 규칙에 따라 규정 준수 여부를 자동으로 평가하는 서비스

    - **Config 규칙 유형**
        - AWS 관리형 규칙: AWS 가 사전 정의한 규칙
        - 사용자 정의 규칙: Lambda 함수로 작성한 커스텀 규칙
    - **보안 필수 config 규칙**
        - restricted-ssh: SSH를 0.0.0.0/0 개방 SG 탐지
    - **자동 교정(Auto Remediation)**
        - config 규칙 위반 시 Automation 문서를 연결하여 자동으로 교정을 할 수 있다.
        - 퍼블릭 S3 버킷 -> 자동으로 Block Public Access 활성화
        - 암호화되지 않은 EBS -> 알림 전송(자동 암호화가 불가능하기 때문에 새 볼륨 생성 필요)
        - SSH 0.0.0.0/0 개발하는 SG -> 자동으로 해당 규칙 제거

- `AWS Security Hub`
    - 여러 보안 서비스의 결과를 중앙에서 통합 관리하는 서비스
    - **보안 표준**
        - `AWS Foundation Security Best Practices`: AWS 자체 보안 모범 사례, 약 200개
        - `CIS AWS Foundations Benchmark`: CIS(Center for Internet Security - 시스템과 클라우드 환경을 안전하게 구성하기 위한) 보안 벤치마크, 약 50개
        - **Ubuntu 서버라면**
            - 불필요한 서비스 비활성화
            - SSH 보안 설정
            - 비밀번호 정책 강화
            - 파일 및 디렉토리 권한 설정
            - 로그 및 감사 설정
            - 방화벽 설정
            - 계정 및 권한 관리
            - 시간 동기화
            - 커널 및 패키지 보안 설정
        - **CIS 보안 점검의 예**
            - SSH의 경우에 보안을 하지 않는다면 SSH가 설치되어 있는가만 확인
            - CIS 보안 점검을 적용한 경우
                - root 직접 로그인 차단 -> 비밀번호 인증 제한 -> 적절한 암호화 알고리즘 사용 -> 접근 가능한 사용자 제한
        - Kubernetes 에서도 CIS가 중요한데 별도의 CIS Kubernetes Benchmark가 존재
        - `PCI DSS`: 결제 카드 산업 보안 표준으로 약 30개 정도
        - `NIST SP 800-53`: 미국 연방 보안 프레임워크로 약 160개 정도
    - **활용 전략**: Security Hub를 활성화하면 자동으로 보안 점수(Security Score)가 산출되는데 이 점수를 기반으로 개선 우선순위를 설정하고 Critical/High 항목부터 순차적으로 대응
- `AWS Audit Manager`
    - AWS 사용에 대한 감사 증거를 자동으로 수집하고 규정 준수 보고서를 생성하는 서비스

    | 기능 | 설명 |
    |----|-----|
    | 사전 정의 프레임워크 | SOC 2, PCI DSS, GCPR, HIPAA 등 표준 프레임워크 제공, 규정 준수 요구 사항에 빠르게 대응 |
    | 자동 증거 수집 | Config, CloudTail, Security Hub 등에서 자동 증거 수집, 수동 증거 수집 노력 대폭 감소 |
    | 커스텀 프레임워크 | 조직 고유의 감사프레임워크 생성 가능, 내부 정책 및 규정에 맞춤 대응 |
    | 감사 보고서 | 감사인에게 제출 가능한 보고서 자동 생성, 감사 준비 시간 단축 |

- `AWS trusted Advisor`
    - AWS 환경의 보안, 비용 최적화, 성능, 서비스 한도를 점검하는 서비스
    - **자동으로 점검하는 항목**
        - S3 버킷 퍼블릭 접근: 퍼블릭 읽기/쓰기가 허용된 S3 버킷 탐지, 무료
        - 루트 계정 MFA: 루트 계정의 MFA 활성화 여부 확인, 무료
        - 보안 그룹 무제한 접근: 0.0.0.0/0 으로 개방된 포트를 가진 SG를 탐지, 무료
        - IAM 액세스 키 로테이션: 90일 이상 로테이션되지 않은 액세스 키 탐지, 유료
        - EBS 퍼블릭 스냅샷: 퍼블릭으로 공유된 EBS 스냅샷 탐지, 유료
        - CloudTrail 로깅: CloudTrail(AWS에서 감사 보안 로그 서비스인데 누가 무엇을 했는지 기록해주는 서비스) 활성화 여부 확인, 유료

- `Amazon Inspector`
    - EC2 인스턴스, 컨테이너 이미지, Lambda 함수의 소프트웨어 취약점과 네트워크 노출을 자동으로 스캔하는 서비스

    | 스캔 대상| 검출 내용 | 특징 |
    |----|-----|------|
    | EC2 인스턴스 | OS 패키지 취약점, 네트워크 도달 가능성 | SSM Agent 기반 에이전트리스 스캔 |
    | ECR 컨테이너 이미지 | 컨테이너 이미지 내 패키지 취약점 | 이미지를 푸시하면 자동 스캔 |
    | Lambda 함수 | 함수 코드 및 종속성 취약점 | 배포 시 자동 스캔 |

- **규정 준수의 코드화**
    - 규정 준수 요구 사항을 코드로 정의하고 자동으로 검증하는 접근 방식

    | 구성 요소 | AWS 서비스 | 역할 |
    |----|-----|-----|
    | 정책 정의 | AWS Config Rules, SCP, Cloud Formation Guard | 보안 정책을 코드로 정의 |
    | 자동 평가 | AWS Config, Security Hub | 리소스가 정책을 준수하는지 지속적 평가 |
    | 자동 교정 | SSM Automation, Lambda | 위반 사항을 자동으로 수정 |
    | 증거 수집 | Audit Manager, CloudTrail | 규정 준수 증거 자동 수집 |
    | 보고 | Security Hub, Audit Manager | 규정 준수 현황 보고서 자동 생성 |

    - **흐름**
        - 정책 코드 작성 -> CI/CD 배포 -> 지속적 평가 -> 자동 교정/보고