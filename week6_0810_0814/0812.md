# Kafka 를 활용한 CQRS

## 1. Kafka

### 1) 개요

- 소스 애플리케이션과 타깃 애플리케이션을 연결하는 파이프라인의 개수가 많아지면서 소스 코드 및 버전 관리에서 이슈가 생겼고 타깃 애플리케이션에 장애가 발생할 경우 그 영향이 소스 애플리케이션에 그대로 전달됨

- LinkedIn 내부 데이터 흐름을 개선하기 위해서 개발
- 각각의 애플리케이션끼리 연결해서 데이터를 처리하는 것이 아니라 한 곳에 모아 처리할 수 있는 중앙 집중화 방식을 사용하는 것이 가능
- 카프카 내부에 데이터가 저장되는 파티션의 동작은 `FIFO(First In First Out)`
- 큐에 데이터를 전송하는 것이 프로듀서이고 큐에서 데이터를 가져가는 것이 컨슈머
- 카프카는 자바로 만들어져서 자바에서 선언 가능한 모든 객체를 지원
- 상용 환경에서는 카프카는 최소 3대 이상의 서버(브로커)에서 분산 운영되며 프로듀서를 통해 전송받은 데이터는 파일 시스템에 안전하게 기록
- 오픈 소스
- Big Data 나 Data Pipeline 구축하는 곳에서는 거의 필수 요소

### 2) 사용하는 이유

- 높은 처리량
- 확장성: 스케일 아웃이나 스케인 인 과정이 쉬움
- 영속성: 데이터를 파일 시스템에 저장하는 것이 가능
- 고가용성

### 3) 데이터 레이크 아키텍쳐

- 전통적인 방식은 각 서비스 애플리케이션이 End To End 로 데이터를 모아서 처리
- 람다 아키텍쳐
- 카파 아키텍쳐
- 스트리밍 데이터 레이크 아키텍쳐

### 4) 설치

- docker 를 이용
- docker-compose 파일 생성(docker-compose.yml)
    
    ```yml
    version: '3.8'
    services:
    zookeeper:
        image: wurstmeister/zookeeper:latest
        container_name: zookeeper
        ports:
        - "2181:2181"
    kafka:
        image: wurstmeister/kafka:latest
        container_name: kafka
        ports:
        - "9092:9092"
        environment:
        - KAFKA_ADVERTISED_HOST_NAME=127.0.0.1
        - KAFKA_ZOOKEEPER_CONNECT=zookeeper:2181
        volumes:
        - /var/run/docker.sock:/var/run/docker.sock
    ```

    - 터미널에서 `docker-compose up -d`
    - 설정 변경
    - 토픽 생성
        - 컨테이너 내부에 접속: `docker exec -it kafka /bin/bash`
        <!-- - 파일을 열어서 수정: `vim /opt/kafka/config/server.properties`
        - 파일이 없는 경우는 생성해서 삽입하는 것도 가능 -->
        - 명령어가 있는 디렉토리로 이동: `cd /opt/kafka/bin`
        - 토픽 생성: `kafka-topics.sh --create --zookeeper zookeeper:2181 --replication-factor 1 --partitions 1 --topic exam-topic`
        - 토픽 확인: `kafka-topics.sh --bootstrap-server localhost:9092 --list`
        - 토픽 삭제: `kafka-topics.sh --delete --zookeeper zookeeper:2181 --topic exam-topic`
    - 토픽에 메시지를 전송하고 확인
        - 프로듀서 생성: `kafka-console-producer.sh --topic exam-topic --broker-list localhost:9092`
        - 별도의 터미널을 열어서 도커 컨테이너 안에 접속한 후 컨슈머 생성: `kafka-console-consumer.sh --topic exam-topic --bootstrap-server localhost:9092 --from-beginning`

- **우분투에 직접 설치**
    - Kafka는 자바로 만들어져 있으므로 JDK 먼저 설치
        - `sudo apt update`
        - `sudo apt install openjdk-17-jdk`
        - `java -version`
    - **kafka 설치**
        - 다운로드: `wget https://archive.apache.org/dist/kafka/3.6.0/kafka_2.13-3.6.0.tgz`
        - `wget https://archive.apache.org/dist/kafka/3.6.0/kafka_2.13-3.6.0.tgz`
        - 압축 해제: `tar xvf kafka_2.13-3.6.0.tgz`
        - 압축을 해제한 디렉토리를 전역 설정으로 이동: `sudo mv kafka_2.13-3.6.0 /opt/kafka`
        - 환경 변수 설정
            - `export KAFKA_HOME=/opt/kafka`
            - `export PATH=$PATH:$KAFKA_HOME/bin`
        - 변경한 내용 바로 적용
            - `source ~/.bashrc`
    - **카프카 힙 메모리 설정**
        - EC2와 같은 클라우드 인프라의 인스턴스에 설치할 때는 메모리를 확인해야 한다.
        - 카프카와 주키퍼를 실행하는데 필요한 메모리가 1.5G
        - 일반적인 인스턴스의 경우 1G 정도의 메모리만 가지고 있으므로 kafka 가 구동되지 않음
        - 전역 환경 변수 설정: `nano ~/.bashrc`
            - `export KAFKA_HEAP_OPTS="-Xmx400m -Xms400m"`
        - 설정 내용 반영: `source ~/.bashrc`
        - 확인: `echo $KAFKA_HEAP_OPTS`
    - **주키퍼 실행**
        - `zookeeper-server-start.sh -daemon /opt/kafka/config/zookeeper.properties`
        - `jps -vm`
    - **카프카 서버 설정**: `config/server.properties`
        - `vim /opt/kafka/config/server.properties`
            ```ini
            listeners=PLAINTEXT://:9092
            advertised.listeners=PLAINTEXT://127.0.0.1:9092 # 실제 ip를 적으면 외부에서 이 kafka를 사용할 수 있다.

            delete.topic.enable=true
            auto.create.topics.enable=true
            ```
    - **카프카 브로커 실행**
        - `kafka-server-start.sh -daemon /opt/kafka/config/server.properties`
        - `jps -m`

### 5. 주요 개념

- `zookeeper`
    - 분산 코디네이터로 최신 버전에서는 자체 관리로 변경이 돼서 zookeeper가 없어도 됨

- `Topic`
    - 카프카에서 데이터를 구분하기 위해 사용하는 단위
    - 1개 이상의 파티션 소유
    - 파티션에는 프로듀서가 보낸 데이터들이 들어가 저장되는데 이 데이터를 `레코드`라고 한다.

- `Partition`
    - 카프카의 병렬 처리 핵심으로써 그룹으로 묶인 컨슈머들이 레코드를 병렬로 처리할 수 있도록 매칭
    - 컨슈머 개수와 파티션 개수를 늘리면 처리량이 증가
    - 자료구조에서 큐와 비슷한 구조

### 6) 파이썬에서 카프카 사용

- 패키지 설치: `pip install kafka-python`

- producer 코드
    ```py
    from kafka import KafkaProducer
    import json

    # 프로듀서 클래스
    class MessageProducer:
        # 프로듀서 초기화
        # broker 는 브로커 위치
        # topic 은 토픽 이름
        def __init__(self, broker, topic):
            self.broker = broker
            self.topic = topic
            self.producer = KafkaProducer(
                bootstrap_servers = self.broker,
                value_serializer = lambda x: json.dumps(x).encode("utf-8"),
                acks = 0,
                api_version = (2, 5, 0),
                key_serializer = str.encode,
                retries = 3
            )

        def send_message(self, msg, auto_close=True):
            try:
                # send는 버퍼에 전송
                future = self.producer.send(self.topic, value=msg, key="key")
                # 실제 전송은 버퍼가 가득차거나 flush를 호출해야 전송된다.
                self.producer.flush()
                if auto_close:
                    self.producer.close()
                future.get(timeout=2) # 비동기 처리라서 조금 대기를 해줘야 함
                return {"status_code": 200, "error": None}
            except Exception as exc:
                raise exc

    # 브로커와 토픽명을 지정
    broker = ["localhost:9092"]
    topic = "exam-topic"
    pd = MessageProducer(broker, topic)

    msg = {"name": "adam", "age": 50}
    res = pd.send_message(msg)
print(res)
    ```

- consumer 코드
    ```py
    from kafka import KafkaConsumer
    import json

    class MessageConsumer:
        def __init__(self, broker, topic):
            self.broker = broker;
            self.consumer = KafkaConsumer(
                topic,
                bootstrap_servers = self.broker,
                value_deserializer = lambda x: x.decode("utf-8"),
                group_id="my-group",
                auto_offset_reset = "earliest",
                enable_auto_commit = True
            )
        
        def receive_message(self):
            try:
                for message in self.consumer:
                    print(message.value)
            except Exception as exc:
                raise exc

    broker = ["localhost:9092"]
    topic = "exam-topic"

    cs = MessageConsumer(broker, topic)
    cs.receive_message()
    ```

## 2. MySQL, MongoDB, Spring Boot, Kafka를 이용한 CQRS 구현

### 0) CQRS

- **개요**
    - `Command and Query Responsibility Segregation`의 약자로 데이터 저장소로부터 읽기와 업데이트 작업을 분리하는 패턴
    - 애플리케이션의 퍼포먼스, 확장성, 보안성을 극대화할 수 있고 여러 요청으로부터 들어온 복수의 업데이트 명령에 대한 충돌도 방지할 수 있음

- **기존 방식의 문제점**
    - 전통적인 아키텍쳐에서는 데이터베이스에서 데이터를 조회하고 업데이트하는데 같은 데이터 모델을 사용
    - 복잡한 애플리케이션에서 이러한 접근 방식은 유지보수를 어렵게 만들 수 있는데, 데이터를 조회할 때 사용하는 데이터의 형태가 다르다면 각기 다른 형태의 DTO(Data Transfer Object - 데이터를 이동할 때 사용하는 클래스)를 반환하는 매우 다양한 쿼리들을 수행할 수 있는데 각각 다른 DTO들에 매핑을 하는 것은 복잡해질 수 있고, 데이터를 쓰거나 업데이트를 할 때는 복잡한 유효성 검사와 비즈니스 로직도 수행해야 하는데 이 모든 걸 하나의 데이터 모델이 한다면 너무 많은 것을 수행하는 복잡한 모델이 되는 것
    - 읽기와 쓰기의 부하는 일반적으로 동일하지 않기 때문에 각각에 대해 다른 성능이 요구됨
    - 읽기와 쓰기의 작업에서 사용되는 데이터 표현들이 서로 일치하지 않는 경우가 많은데, 그로 인해 일부 작업에서는 필요하지 않은 추가적인 컬럼이나 속성의 업데이트가 이루어져야 하는 경우가 있음
    - 동일한 데이터 세트에 대해 병렬로 작업이 수행될 때 데이터 경합이 발생
    - 정보 조회를 위해 요구되는 복잡한 쿼리로 인해 성능에 부정적인 영향을 줄 수 있음

- **해결책**
    - 읽기와 쓰기를 각각 다른 모델로 분리하는데 Command를 이용해서 데이터를 수정하고 Query를 이용해서 데이터를 조회
    - Command는 데이터 중심적이 아니라 작업 중심이 되어야 함
    - Command는 동기적으로 처리되기보다는 비동기적으로 큐에 쌓인 후 진행
    - Query는 절대로 디비를 수정하지 않고 어떤 도메인 로직도 캡슐화하지 않은 DTO만을 반환
    - CQRS를 이용하는 경우 ORM 툴을 통해서 DB 스키마로부터 자동으로 생성되도록 할 수 없다는 단점이 있음.
    - 작업에 특화된 데이터베이스를 사용하는 것이 가능

- **장점**
    - 독립적인 스케쥴링
    - 최적화된 데이터 스키마를 사용: 읽기 저장소는 쿼리에 최적화된 스키마를 사용할 수 있고 쓰기 저장소는 쓰기에 최적화된 스키마를 사용할 수 있음
    - 보안
    - 관심사 분리: 대부분의 복잡한 비즈니스 로직은 쓰기 모델에 들어가고 읽기 모델은 상대적으로 간단한 조회 로직만 포함
    - 간단한 쿼리: 읽기 저장소의 materialized view를 통해 복잡한 조인문을 사용하지 않을 수 있음

- **구현 이슈**
    - 복잡성: 아이디어는 간단하지만 이 패턴은 이벤트 소싱 패턴을 포함할 경우 복잡해질 수 있음
    - 메세징: 명령을 수행하고 업데이트 이벤트를 발행하는 것이 보편적인 사용법인데 이 경우 애플리케이션은 반드시 메시지 실패나 중복 메시지 처리를 해야 함
    - 데이터 일관성

- **구현해야 하는 경우**
    - 많은 사용자가 동일한 데이터에 병렬로 액세스 하는 경우
    - 복잡한 프로세스나 도메인 모델을 통해 가이드 되는 작업 기반 사용자 인터페이스
    - 데이터의 읽기의 성능이 데이터 쓰기의 성능과 별도로 조정이 가능해야 할 때
    - 쓰기와 읽기를 분리해서 구현할 떄
    - 하나의 시스템이 장기간 여러 버전을 가질 수 있고 정기적으로 수정되는 경우
    - 다른 시스템과 통합할 때 

- **권장되지 않는 경우**
    - 도메인이나 비즈니스 로직이 간단할 때
    - 단순한 CRUD 일 때

### 1) 준비

- 디비 설치하고 접속 확인
- Kafka 설치
- JDK 설치
- 인텔리제이 설치

### 2) 쓰기 프로젝트

- Spring Dev Tools, Lombok, Spring Web, Spring Data JPA, MySQL, Kafka 의존성을 가지고 프로젝트를 생성
- 설정 파일 수정: `application.yml`

    ```yml
    server:
      port: 7000
    spring:
      application:
        name: Write
      datasource:
        url: jdbc:mysql://localhost:3306/mysql
        driver-class-name: com.mysql.cj.jdbc.Driver
        username: root
        password: wnddkd
      jpa:
        hibernate:
          ddl-auto: update
        properties:
          hibernate:
            format_sql: true
            show_sql: true

    logging:
      level:
        org.hibernate.type.description.sql: trace
    ```

- Entity 클래스(`Book`) 생성: 데이터베이스 테이블과 직접 연결되는 클래스

    ```java
    import jakarta.persistence.*;
    import lombok.*;

    import java.util.Date;

    @Entity
    @Table(name = "book")
    @ToString
    @Getter
    @Builder
    @AllArgsConstructor
    @NoArgsConstructor
    public class Book {
        @Id
        @GeneratedValue(strategy = GenerationType.AUTO)
        private Long bid;

        @Column(length = 50, nullable = false)
        private String title;
        @Column(length = 50, nullable = false)
        private String author;
        @Column(length = 50, nullable = false)
        private String category;
        @Column
        private int pages;
        @Column
        private int price;
        @Column
        private Date published_date;
        @Column(length = 50, nullable = false)
        private String description;
    }
    ```

- DTO(Data Transfer Object - 계층 간의 데이터 이동을 위한 클래스) 클래스(`BookDTO`)

    ```java
    import lombok.Data;

    @Data
    public class BookDTO {
        private String title;
        private String author;
        private String category;
        private int pages;
        private int price;
        private String published_date;
        private String description;
    }
    ```

- 데이터베이스 연동하는 인터페이스 생성: `BookRepository`

    ```java
    import org.springframework.data.jpa.repository.JpaRepository;

    public interface BookRepository extends JpaRepository<Book, Long> {
    }
    ```

- 서비스 클래스를 생성: `BookService`

    ```java
    import lombok.RequiredArgsConstructor;
    import org.springframework.stereotype.Service;

    import java.text.SimpleDateFormat;
    import java.util.Date;
    import java.util.Locale;

    @Service
    @RequiredArgsConstructor
    public class BookService {
        private final BookRepository bookRepository;

        public void saveBook(BookDTO bookDTO){
            try {
                SimpleDateFormat formatter = new SimpleDateFormat(
                        "yyyy-MM-dd", Locale.ENGLISH);
                Date published_date = formatter.parse(bookDTO.getPublished_date());
                Book book = Book.builder()
                        .title(bookDTO.getTitle())
                        .author(bookDTO.getAuthor())
                        .category(bookDTO.getCategory())
                        .pages(bookDTO.getPages())
                        .price(bookDTO.getPrice())
                        .published_date(published_date)
                        .description(bookDTO.getDescription())
                        .build();
                bookRepository.save(book);
            }
            catch(Exception e){
                System.out.println(e.getMessage());
            }
        }
    }
    ```

- 사용자의 요청을 처리하는 `BookController`

    ```java
    import lombok.RequiredArgsConstructor;
    import org.springframework.web.bind.annotation.GetMapping;
    import org.springframework.web.bind.annotation.PostMapping;
    import org.springframework.web.bind.annotation.RequestBody;
    import org.springframework.web.bind.annotation.RestController;

    @RestController
    @RequiredArgsConstructor
    public class BookController {
        private final BookService bookService;

        @GetMapping("/")
        public String index(){
            return "homepage";
        }

        @GetMapping("/health")
        public String healthCheck(){
            return "success";
        }

        @PostMapping("/cqrs/book")
        public String saveBook(@RequestBody BookDTO bookDTO){
            bookService.saveBook(bookDTO);
            return "success";
        }
    }
    ```

- 웹 서버 구동: `localhost:7000` 으로 접속

- 데이터 삽입 테스트: `POSTMAN API`(REST API Server 테스트에 유용)

### 3) 읽기 프로젝트

- 프로젝트 생성(Spring Boot Devtools, Lombok, Spring Web, Spring Data JPA, Spring Data MongoDB, Kafka)

- Controller 클래스 생성: `BookController`

    ```java
    import com.mongodb.client.*;
    import lombok.RequiredArgsConstructor;
    import org.bson.Document;
    import org.springframework.http.HttpStatus;
    import org.springframework.http.ResponseEntity;
    import org.springframework.web.bind.annotation.GetMapping;
    import org.springframework.web.bind.annotation.RestController;
    import java.util.ArrayList;
    import java.util.List;

    @RestController
    @RequiredArgsConstructor
    public class BookController {
        @GetMapping("/cqrs/book")
        public ResponseEntity<List> getBooks(){
            MongoClient mongoClient = MongoClients.create("mongodb://localhost:27017");
            MongoDatabase database = mongoClient.getDatabase("mymongo");
            MongoCollection<Document> mongo_books =
                    database.getCollection("books");
            List<Document> list = new ArrayList<Document>();

            try{
                try(MongoCursor<Document> cur = mongo_books.find().iterator()){
                    while(cur.hasNext()){
                        Document doc = cur.next();
                        list.add(doc);
                    }
                }
            }catch(Exception e){
                e.printStackTrace();
            }
            finally{
                mongoClient.close();
            }
            return ResponseEntity.status(HttpStatus.OK).body(list);
        }
    }
    ```

- 실행하고 확인

- 내일 시작할 때 환경
    - Java
    - 인텔리제이
    - Docker
    - MySQL
    - MongoDB
    - Git