# Docker

## 5. 도커 볼륨

### 5) tmpfs mount

- **개요**
    - 일시적인 마운트
    - 호스트 컴퓨터의 메모리에 지속되는 데이터를 활용하고자 할 때 사용
    - 컨테이너 실행 시 지정해서 사용하고 컨테이너 제거 시 자동해제
    - 생성 시 --tmpfs 컨테이너의 경로
- **생성**
    - httpd:2(apache 웹 서버) 이미지를 가지고 컨테이너를 생성하되 /var/www/html 디렉토리를 마운트
        - `docker run --name webserver -dit --tmpfs /var/www/html httpd:2`

### 6) MySQL 영속성 유지

- **기본 정보**
    - 이미지: `mysql`
    - 모든 버전 확인: `curl -s "https://registry.hub.docker.com/v2/repositories/library/mysql/tags?page_size=100" | jq -r '.results[].name'`
    - 컨테이너 포트 번호: `3306`
    - 데이터 디렉토리: `/var/lib/mysql`
    - 환경 변수
        - `MYSQL_ROOT_PASSWORD` 를 이용해서 루트 계정의 비밀번호를 설정
        - `MYSQL_DATABASE`를 이용해서 기본 데이터베이스를 생성 가능

- **생성**
    ```sh
    docker run --name mysql-volume -dit \
        -e MYSQL_ROOT_PASSWORD=1234 \
        -e MYSQL_DATABASE=itstudy \
        -v mysql-data-vol:/var/lib/mysql \
        mysql
    ```
    - Mac의 경우 이미지를 컨테이너화 하지 못하면 `--platform linux/amd64` 를 추가해서 실행
- **확인**
    - 컨테이너 생성 여부 확인: `docker ps -a`
    - 컨테이너 실행 여부 확인: `docker ps`

    - **볼륨 확인**
        - `docker volume ls`
        - `docker volume inspect 볼륨이름`

        - `docker inspect 컨테이너이름 --format '{{json .Mounts}}' | jq`

- **데이터베이스 작업**
    - 컨테이너 내부로 접속: `docker exec -it 컨테이너이름 /bin/bash`
    - 데이터베이스 접속: `mysql -uroot -p`
    - 샘플 데이터 생성
        - `use itstudy;`
        - `create table mytable(num int, name char(30));`
        - `insert into mytable values(1, "adam");`
        - `select * from mytable;`
    - 컨테이너 중지 및 삭제
        - `docker stop 컨테이너이름`
        - `docker rm 컨테이너이름`

- **컨테이너를 다시 생성해서 확인(볼륨 말고는 다 달라도 괜찮다)**
    ```sh
    docker run --name mysql-volume -dit \
        -e MYSQL_ROOT_PASSWORD=1234 \
        -e MYSQL_DATABASE=itstudy \
        -v mysql-data-vol:/var/lib/mysql \
        mysql
    ```
    - 컨테이너 내부로 접속: `docker exec -it 컨테이너이름 /bin/bash`
    - 데이터베이스 접속: `mysql -uroot -p`
    - 데이터베이스 확인
        - `use itstudy;`
        - `SELECT * FROM mytable;`
    - 참고: 볼륨에 이미 초기화된 데이터가 있으면 `MYSQL_ROOT_PASSWORD`, `MYSQL_DATABASE` 환경변수는 무시됨. 실제 root 비밀번호는 최초 초기화 시점 값(`1234`)이 계속 유지됨

### 7) nginx의 로그를 호스트 컴퓨터의 디렉토리에 저장

- **nginx 기본 정보**
    - 이미지: `nginx`
    - 공개된 컨테이너 포트: `80`
    - 로그 디렉토리: `/var/log/nginx`

- **로그를 저장할 디렉토리를 생성: 홈 계정에 logs**
- **컨테이너 생성**
    ```bash
    docker run \
      --name nginx-volume \
      -dit \
      -v /home/etakyung/logs:/var/log/nginx \
      -p 8000:80 \
      nginx
    ```
    - 외부에서 접속
    - 호스트 컴퓨터의 디렉토리를 확인해서 로그 파일이 있는지 확인

### 7) 볼륨의 다른 용도

- 볼륨은 컨테이너 간의 통신 용도로 사용하는 것이 가능
- 볼륨을 생성하고 여러 컨테이너가 이 볼륨을 공유하면 볼륨을 이용해서 데이터 공유가 가능
- 데이터 공유 컨테이너를 이용한 컨테이너 간의 데이터 공유
    - **컨테이너 생성**
        - `docker create -v /data-volume --name=datavol ubuntu:20.04`
    - **공유 연결할 컨테이너 생성**
        - `docker run -it --volumes-from datavol --name=datavol-1 ubuntu:20.04`
        - `echo 'datavol-1 container' > /data-volume/test-volume.txt`
    - **컨테이너 중지됐을 때 다시 실행**
        - `docker start -ai datavol-1`
    - **공유 연결할 컨테이너 생성**
        - `docker run -it --volumes-from datavol --name=datavol-2 ubuntu:20.04`
        - `ls /data-volume`
        - `echo 'datavol-2 container' > /data-volume/test-volume.txt`
    - datavol-1, datavol-2 두 컨테이너가 `--volumes-from datavol` 옵션을 통해 datavol 컨테이너의 `/data-volume` 볼륨을 공유함
        - 즉, 한 컨테이너(datavol-1)에서 작성한 파일(test-volume.txt)을 다른 컨테이너(datavol-2)에서도 그대로 확인 가능

### 8) 파일 시스템 마운트의 한계

- **디렉토리를 통째로 마운트하는 경우 (대체)**
    - 컨테이너의 마운트 대상 디렉토리가 이미 존재하고 이미지 레이어에 이 디렉토리의 파일이 포함되어 있는 경우, 이미 존재하는 대상 디렉토리에 마운트하면 마운트의 원본 디렉토리가 기존 디렉토리를 완전히 **대체**해서 이미지에 포함되어 있던 원래 파일은 사용할 수 없음
    - 이미지 레이어에서 완전히 삭제되는 것은 아니지만, 마운트된 디렉토리에 가려져서 컨테이너 실행 중에는 접근이 불가능함
    - 예: nginx 이미지에는 `/usr/share/nginx/html` 안에 기본 index.html이 들어있는데, 이 경로에 빈 호스트 디렉토리를 통째로 마운트하면 기존 index.html이 사라진 것처럼 보임
        ```sh
        docker run -dit --name nginx-test -v /home/etakyung/empty-dir:/usr/share/nginx/html nginx
        ```
    - 주의점: 초기 설정 파일이나 기본 콘텐츠가 들어있는 디렉토리를 통째로 마운트할 때는, 호스트 디렉토리에도 필요한 파일을 미리 준비해 둬야 함(그렇지 않으면 컨테이너의 해당 기능이 정상 동작하지 않을 수 있음)

- **파일 하나만 마운트하는 경우 (병합처럼 보임)**
    - 호스트 컴퓨터의 파일 하나를 컨테이너에 이미 존재하는 디렉토리 내의 특정 파일 경로로 마운트하는 경우, 디렉토리 자체가 교체되는 게 아니라 지정한 파일 하나만 교체됨
    - 그 결과 같은 디렉토리 안의 나머지 파일은 이미지에서 온 그대로 유지되고, 마운트한 파일만 호스트의 내용으로 바뀌어서 두 출처(이미지 + 호스트)의 파일이 한 디렉토리 안에 함께 나타나는 것처럼 보임
    - 예: nginx 설정 파일 하나만 호스트 것으로 교체
        ```sh
        docker run -dit --name nginx-conf -v /home/etakyung/nginx.conf:/etc/nginx/nginx.conf nginx
        ```
        - 이 경우 `/etc/nginx` 디렉토리 안의 다른 이미지 파일들(mime.types 등)은 그대로 남아있고 nginx.conf만 호스트의 파일로 바뀜

- **핵심 요약**

    | 마운트 대상 | 동작 방식 | 결과 |
    | --- | --- | --- |
    | 디렉토리 → 디렉토리 | 대체(overwrite) | 기존 디렉토리 내용이 전부 가려짐 |
    | 파일 → 디렉토리 내 특정 파일 | 교체(replace) | 지정한 파일만 바뀌고 나머지는 유지(병합처럼 보임) |

## 6. 도커 네트워크

### 1) 개요

- 도커 컨테이너는 기본적으로 외부와 격리가 되어 있는데 다른 컨테이너와 통신을 할 수 있도록 해주는 매커니즘

- Docker Network의 하위 시스템 연결을 위해 Docker Network Driver를 이용
- Docker를 설치하게 되면 기본적으로 제공되는 `docker0` 라는 소프트웨어적으로 구현된 가상 이더넷 브릿지 네트워크를 이용해서 통신
- 모든 컨테이너는 기본적으로 docker0 브릿지에 연결되어 172.17.0.0/16 의 CIDR(Classless Inter-Domain Routing) 범위로 IP 주소가 할당
- **네트워크 인터페이스 카드**
    - `enp0s8`: Ubuntu 의 리눅스 네트워크 카드로 VirtualBox 에서 제공하는 IP 주소를 할당받아서 사용 중
    - `docker0`
        - 도커를 설치할 때 만들어지는 브릿지 네트워크
        - 이 브릿지 네트워크가 컨테이너 DHCP 형태로 IP 할당
    - `eth0`
        - docker container에 생성되는 네트워크 인터페이스 카드
        - docker0 를 게이트웨이로 해서 순차적으로 IP를 할당받거나 사용자가 동일한 대역의 IP 주소를 지정할 수 있음
        - vethXXXXXX: eth0 와 쌍으로 생성되는데 docker0 와 가상의 터널링 네트워크를 제공

### 2) 기본 브릿지 네트워크 모드

- 드라이버 확인: `docker network ls`
- 네트워크를 자세히 조회: `docker network inspect bridge`
- 컨테이너 2개 생성
    - `docker run -dit --name container1 ubuntu:20.04`
    - `docker run -dit --name container2 ubuntu:20.04`

- 컨테이너의 IP 확인
    - `docker inspect container1 | grep IPAddress`

### 3) 컨테이너의 포트가 브릿지 모드의 네트워크와 연결되는 과정

- nginx 컨테이너를 생성: `docker run -d --name nginx -p 8000:80 nginx`
- 브라우저에서 접속
- **접근 과정**
    - 브라우저에서 `http://호스트IP:연결포트` 로 접속
    - 호스트 운영체제에서는 포트가 열려있는지 확인하고 포트가 열려있으면 포트에 연결된 컨테이너를 찾음
    - 도커 브릿지 네트워크에서 그 포트에 연결된 private ip 와 포트 번호로 변환이 발생하는데(NAT) 여기서는 포트 변환까지 같이 수행

### 4) 네트워크 관련 명령어

- `connect`: 네트워크에 컨테이너를 새로이 접속
- `disconnect`: 네트워크에서 컨테이너의 접속을 끊음
- `create`: 네트워크를 생성
- `inspect`: 네트워크의 상세 정보를 확인
- `ls`: 네트워크 목록 출력
- `prune`: 사용되지 않는 네트워크를 삭제
- `rm`: 네트워크 삭제

### 5) 여러 개의 컨테이너 연동

- **MySQL 컨테이너 실행**
    - 인자
        - `MYSQL_ROOT_PASSWORD`
        - `MYSQL_DATABASE`
        - `MYSQL_USER`
        - `MYSQL_PASSWORD`
    - 옵션
        - `net`: 네트워크 연결
        - `character-set-server`: 문자 인코딩
        - collation-server: 정렬 순서

- **wordpress**
    - 개요
        - 웹 사이트를 만들기 위한 소프트웨어
        - 이 소프트웨어는 apache 웹 서버 그리고 데이터베이스와 PHP 런타임으로 구성
        - 데이터베이스는 MySQL 과 MariaDB 만 지원
        - 도커 허브에 있는 공식 이미지는 apache web server 와 php 런타임을 내장하고 있는데 데이터베이스는 내장하고 있지 않아서 MySQL이나 MariaDB 컨테이너를 생성해서 연결을 해주어야 한다. 
- **MySQL 과 Wordpress 컨테이너를 만들어서 연결**
    - 네트워크 생성: `docker network create wordnetwork`
    - 위에서 만든 네트워크에 연결된 MySQL 컨테이너 생성
        ```bash
        docker run --name mysql -dit \
            --net=wordnetwork \
            -e MYSQL_ROOT_PASSWORD=1234 \
            -e MYSQL_DATABASE=wordpress \
            -e MYSQL_USER=adam \
            -e MYSQL_PASSWORD=1234 \
            mysql \
            --character-set-server=utf8mb4 \
            --collation-server=utf8mb4_unicode_ci
        ```
    - MySQL 컨테이너를 사용하는 wordpress 컨테이너를 생성
        ```bash
        docker run \
            --name=wordpress -dit \
            --net=wordnetwork \
            -p 8000:80 \
            -e WORDPRESS_DB_HOST=mysql \
            -e WORDPRESS_DB_NAME=wordpress \
            -e WORDPRESS_DB_USER=adam \
            -e WORDPRESS_DB_PASSWORD=1234 \
            wordpress
        ```
    - 정리
        - `docker stop wordpress mysql`
        - `docker rm wordpress mysql`
        - `docker network rm wordnetwork`

### 개인 작업 번외*) Docker Hub 이미지 배포 테스트

- **개요**: Docker Hub에 올린 이미지(`etakyung/for-docker-java:latest`)를 pull 받아 MySQL과 연동해서 동작 확인
- **네트워크 생성 및 MySQL 실행**
    ```bash
    docker network create for-java-net

    docker run -d --name mysql-test --network for-java-net \
      -e MYSQL_DATABASE=itemdb -e MYSQL_USER=user1 -e MYSQL_PASSWORD=1234 -e MYSQL_ROOT_PASSWORD=1234 \
      mysql:8.4
    ```
    - `docker logs -f mysql-test`로 `ready for connections` 로그 확인 후 Ctrl+C
- **이미지 pull 및 앱 컨테이너 실행**
    ```bash
    docker pull etakyung/for-docker-java:latest

    docker run -d --name app-test --network for-java-net \
      -p 8080:8080 \
      -e SPRING_DATASOURCE_URL=jdbc:mysql://mysql-test:3306/itemdb \
      -e SPRING_DATASOURCE_USERNAME=user1 \
      -e SPRING_DATASOURCE_PASSWORD=1234 \
      etakyung/for-docker-java:latest
    ```
    - `docker logs -f app-test`로 `Started ForDockerApplication` 로그 확인
- **API 테스트**
    ```bash
    curl -X POST http://localhost:8080/api/items \
      -H "Content-Type: application/json" \
      -d '{"name":"test-item","description":"docker hub test"}'

    curl http://localhost:8080/api/items
    ```
- **정리**
    ```bash
    docker rm -f app-test mysql-test
    docker network rm for-docker-net
    ```

## 7. Dockerfile

### 1) IaC(Infrastructure as Code, IaC)

- **수동으로 배포했을 때의 발생할 수 있는 문제점**
    - CLI 기반의 인프라 구성 시 사용자 실수 등의 인적 오류 가능성이 높은데 APM(Apache + PHP + MySQL)을 구성하는 경우 설치 순서와 상호 연관성을 고려해 각종 라이브러리와 함께 복잡한 명령어를 고민해야 함
    - 워드프레스의 경우 MySQL 이나 MariaDB 가 구동 중이 아니면 설치도 안되고 실행도 안됨

    - 각종 환경 설정 정보와 설치 프로그램을 요구사항에 맞게 살펴봐야 하는데 만일 설치 이후에 잘못된 설정이 있으면 수정해야 하고 때로는 재설치도 불가피
- 이에 대한 해결책이 IaC

### 2) Dockerfile 의 개요

- `Docker Image` 를 생성하기 위한 스크립트(설정 파일)
- **장점**
    - 이미지를 어떻게 만들었는지를 기록
    - 배포에 용이
    - 이미지가 컨테이너로 생성될 때 특정 행동을 수행하도록 할 수 있음

### 3) 작성 명령어

- **FROM**
    - 필수 항목으로 베이스 이미지를 지정
    - hub.docker.com 에서 제공하는 이미지를 권장
    - 태그를 선택할 때는 작은 크기의 이미지(slim)와 리눅스 배포판인 알파인(alpine) 이미지를 권장하지만 모든 애플리케이션이 동일하지는 않음
    - 태그를 생략하면 `latest`
    - **예시**
        - `FROM ubuntu:20.04`
        - `FROM python:3.9-slim-buste`

- **MAINTAINER**
    - 이미지를 빌드한 작성자 이름과 이메일을 작성
    - 예시
        - `MAINTAINER adam.park itstudy@kakao.com`
- **LABEL**
    - 이미지 작성 목적을 나열하는데 버전, 타이틀, 설명, 라이선스 정보를 작성
    - 여러 개 작성 가능
    - 얘시
        - `LABEL version='1.0'`
        - `LABEL description='web service'`
- **RUN**
    - 설정된 기본 이미지에 패키지 업데이트, 각종 패키지 설치, 명령 실행 등을 작성
    - 여러 개 작성 가능
    - **권장 사항**
        - 다단계 빌드 사용
        - RUN 명령어 별로 레이어가 생성되므로 레이어의 개수를 줄이기 위해서 여러 설치 명령을 연결해서 사용
        - autoremove, autoclean, rm -rf /var/lib/apt/lists/* 을 사용해서 apt 캐시를 삭제: 이미지 크기 감소
    - 사용 방식은 exec 방식과 shell 방식
    - **exec 방식**
        - `RUN ["/bin/bash", "-c", "apt update"]`
        - `RUN ["/bin/bash", "-c", "apt -y install nginx git vim curl"]`
    - **Shell 방식**
        ```bash
        RUN apt update && apt install -y \
        nginx \
        git \
        vim \
        curl && \
        apt clean && \
        apt autoremove -y && \
        rm -rfv /tmp/* /var/lib/apt/lists/* /var/tmp/*
        ```

- **CMD**
    - 생성된 이미지를 컨테이너로 만들 때 실행하는 명령이고 ENTRYPOINT 명령문으로 지정된 커맨드에 디폴트로 넘길 파라미터를 지정할 때 사용
    - 여러 개의 CMD를 작성해도 마지막 하나만 처리
    - 사용 예
        - CMD ["/usr/sbin/apachectl", "-D", "FOREGROUND"]
        - CMD apachectl -D FOREGROUND
        - CMD ["nginx", "-g", "daemon off;"]
        - CMD ["python", "app.py"]
- **ENTRYPOINT**
    - CMD와 마찬가지로 생성된 이미지가 컨테이너로 실행될 때 사용되지만 컨테이너가 실행될 때 명령어 및 인자 값을 전달하여 실행한다는 점이 다름
    - 여러 개의 CMD를 사용하는 경우 ENTRYPOINT 와 같이 사용
    - `ENTRYPOINT` 에 커맨드를 지정하고 CMD는 기본 명령을 지정하면 탄력적으로 이미지를 실행할 수 있음
    - python 명령을 기본으로 runapp.py를 실행하고자 할 떄
        - `ENTRYPOINT ["python"]`
        - `CMD ["runapp.py"]`
    - 동일한 환경에 entrypoint.sh 쉘 스크립트를 이미지에 넣고 실행 권한 설정한 후 컨테이너를 실행할 때 entrypoint.sh 파일을 실행
        - `ADD ./entrypoint.sh /entrypoint.sh`
        - `RUN chmod +x /entrypoint.sh`
        - `ENTRYPOINT ["/bin/bash", "/entrypoint.sh"]`
    - `ENTRYPOINT` 는 Docker 컨테이너 실행 시 항상 수행해야 하는 명령어를 지정하고 `CMD`는 Docker 컨테이너 실행 시 다양한 명령어를 지정하는 경우 유용
    - **CMD와 결합했을 때 인자가 대체되는 방식**
        - `ENTRYPOINT ["python"]` + `CMD ["runapp.py"]` 로 빌드된 이미지를 그냥 실행하면 → `python runapp.py`
        - `docker run 이미지이름 other.py` 처럼 컨테이너 실행 시 명령 뒤에 인자를 붙이면 CMD("runapp.py")만 그 인자("other.py")로 교체됨 → `python other.py`
        - 즉 ENTRYPOINT는 고정된 실행 파일, CMD는 런타임에 바뀔 수 있는 기본 인자 역할
    - **exec 형식 vs shell 형식**
        - exec 형식(권장): `ENTRYPOINT ["python", "app.py"]` → 프로세스가 PID 1로 직접 실행되어 `docker stop`이 보내는 SIGTERM을 바로 수신
        - shell 형식: `ENTRYPOINT python app.py` → 내부적으로 `/bin/sh -c`로 감싸져 실행되며, 실제 프로세스는 자식 프로세스가 되어 신호 전달이 한 단계 더 거쳐감(정상 종료가 늦어질 수 있음)
    - **재정의**: `docker run --entrypoint 새명령 이미지이름` 옵션으로 이미지에 설정된 ENTRYPOINT를 런타임에 덮어쓸 수 있음
    - **여러 개 작성 시**: CMD와 동일하게 여러 번 작성해도 마지막 ENTRYPOINT만 적용됨

- **COPY**
    - 호스트 환경의 파일을 이미지 안에 복사하는 경우 작성
    - 빌드 작업 디렉토리 외부의 파일은 COPY 할 수 없음
    - 예시
        - `COPY index.html /usr/share/nginx/html`
        - `COPY ./runapp.py /`
- **ADD**
    - 호스트 환경의 파일, 디렉토리를 이미지 안에 복사하는 경우뿐만 아니라 URL 주소에서 직접 다운로드 받아서 넣을 수도 있고 압축 파일인 경우는 지정한 경우로 압축을 풀어서 추가
    - **예시**
        - `ADD index.html /usr/share/nginx/html`
        - `ADD URL /workspace/data/`

- **ENV**
    - 이미지 안에 각종 환경 변수를 지정하는 경우 사용
    - 애플리케이션 사용을 쉽게 하기 위해 사전에 구성되어야 하는 환경 변수들이 있는 경우 사용
    - 자바 홈 디렉토리, 특정 실행 파일의 경로를 보장하기 위해 절대 경로를 지정하기 위한 PATH 설정, 프로그램 버전 등을 사전에 설정
    - Dockerfile 에서 ENV를 설정하면 RUN, WORKDIR 등에서 환경 변수를 사용해 반복을 피할 수 있음
    - 반복된 표현이 사용되는 경우에도 환경 변수 설정을 권장
    - 사용 예시
        - `ENV JAVA_HOME /usr/lib/jvm/java-8-oracle`
- **EXPOSE**
    - 컨테이너가 호스트 네트워크를 통해 들어오는 트래픽을 listening 하는 포트와 프로토콜을 지정하기 위해서 작성
    - docker run 을 할 때 -p 에 설정해서 외부와 연결
    - 예시
        - `EXPOSE 80`
        - `EXPOSE 80/tcp`

- **VOLUME**
    - 볼륨을 이미지 빌드에 미리 설정하는 경우에 작성
    - VOLUME으로 지정된 컨테이너의 경로는 볼륨의 기본 경로인 /var/lib/docker 와 자동으로 연결
    - **사용 예시**
        - `VOLUME /var/log`
        - `VOLUME /var/www/html`
- **USER**
    - 컨테이너의 기본 사용자는 `root`
    - 애플리케이션이 권한없이 서비스를 실행할 수 있다면 USER를 통해 다른 사용자로 변경해서 사용
    - 예시
        - RUN ["useradd", "adam"]
        - USER adam
        - RUN ["/bin/bash", "-c", "date"]
- **WORKDIR**
    - 컨테이너 상에서 작업할 경로를 전환할 때 사용
    - WORKDIR을 설정하면 RUN, CMD, ENTRYPOINT, COPY, ADD 명령문은 해당 디렉토리를 기준으로 실행
    - 지정한 경로가 없으면 자동 생성되고 컨테이너 실행 이후 컨테이너에 접속하면 지정한 경로로 연결
    - **사용 예**
        - `WORKDIR /workspace`
- **ARG**
    - docker build 시점에서 변수의 값을 전달하기 위해 --build-arg=인자를 정의해서 사용
    - 비밀키, 계정 비밀번호 같은 민감한 정보 사용 시 이미지에 직접 작성하면 외부로 노출될 위험이 있어서 빌드할 때 대입해서 사용
    - 사용 예시
        - Dockerfile에 ARG 변수를 정의: `ARG db_name`
        - 빌드할 때: `docker build --build-arg db_name=itstudy .`
        - **주의: ARG는 빌드 시점(RUN 등)에서만 유효하고 컨테이너 실행 시점(CMD, ENTRYPOINT)에는 사라짐**
            - 컨테이너 실행 중에도 값을 쓰려면 ENV로 다시 담아둬야 함: `ENV DB_NAME=$db_name`
            - 이렇게 담아둔 값을 CMD에서 사용할 때: `CMD db_start.sh -h 127.0.0.1 -d $DB_NAME`
- **ONBUILD**
    - 처음 이미지 빌드에는 사용하지 않지만 해당 이미지가 다른 이미지의 기본 이미지로 사용되는 경우 실행될 명령어를 지정할 때 사용
- **STOPSIGNAL**
    - docker stop 명령은 컨테이너에게 SIGTERM 을 보내 정지시킴
    - 다른 시그널을 보내고자 할 때
        - `STOPSIGNAL SIGKILL(시그널 번호 또는 이름)`
- **SHELL**
    - Dockerfile 내부에서 사용할 기본 쉘을 지정하는 경우 작성
    - 사용 예시
        - `SHELL ["/bin/bash", "-c"]`
- **HEALTHCHECK**
    - 컨테이너의 프로세스 상태를 체크하고자 하는 경우 사용
    - 하나의 명령만이 유효하고 여러 개 지정된 경우 마지막에 선언된 것만 적용
    - **옵션**
        - `--interval=초`: 헬스 체크 간격으로 기본값은 30s
        - `--timeout=초`: 타임 아웃으로 기본값은 30s
        - `--retries=숫자`: 타임 아웃 횟수로 기본값은 3
    - **상태 코드**
        - 0: 컨테이너 정상 작동
        - 1: unhealthy(컨테이너가 올바르게 작동하지 않는 상태)
        - 2: strating(예약된 코드)
    - **사용 예**
        - 1분마다 CMD에 있는 명령을 수행해서 3초 이상이 소요되면 한 번의 실패로 간주하고 5번 실패하면 unhealthy로 변경
        - `HEALTHCHECK --interval=1m --timeout=3s --retries=5 CMD curl -f http://localhost || exit 1`

### 4) Dockerfile 명령어 작성 방법

- 시작은 FROM 명령부터 작성하고 다음부터는 순서가 없지만 명령 순서는 빌드 캐시의 무효화와 관련되기 때문에 변경 빈도수가 적은 명령을 먼저 배치하는 것을 권장
- 도커 허브에는 많은 이미지가 존재하는데 무조건 OS 이미지를 선택하고 설치하는 것보다는 원하는 애플리케이션 패키지가 이미 설치된 이미지 선택을 권장하는데 이유는 이미지 관리의 유지보수 측면에서 시간 절약에 도움이 됨

### 5) 이미지 빌드 명령

`docker build [옵션] 이미지이름[:태그] Dockerfile_경로 | URL | 압축파일`

- **옵션**
    - t: 태그를 지정하고자 하는 경우
    - f: Dockerfile이 아니라 다른 이름을 사용한 경우 이름을 지정
- **경로**
    - 디렉토리 단위로 개발하는 것을 권장: 디렉토리에 하나의 Dockerfile 만 존재하기를 권장
    - 현재 디렉토리이면 .을 이용
    - github 같은 분산 버전 관리 시스템을 이용하는 경우에는 URL로 입력
        - `docker build -t phpserver:2.0 github.com/brayanlee/docker-phpserver`
    - 압축 파일에 있는 경우
        - `docker build -f 압축파일경로`

### 6) 아파치 웹 서버에 메인 페이지를 변경하는 이미지

- **작업 디렉토리를 생성하고 index.html 작성**
    ```html
    <html>
        <head>
            <title>Docker Image Build</title>
        </head>
        <body>
            <h1>Apache Web Server</h1>
        </body>
    </html>
    ```
- **Dockerfile 작성**
    ```dockerfile
    FROM httpd
    COPY index.html /usr/local/apache2/htdocs
    ```

- **이미지 빌드**
    - `docker build -t apacheex .`
- **이미지 확인**
    - `docker images`
- **새로 만들어진 이미지를 가지고 컨테이너를 생성**
    - `docker run --name apacheex -d -p 8000:80 apacheex`
- **컨테이너 생성 여부를 확인하고 포트 개방 여부를 확인**

### 7) nginx 를 이용한 프론트엔드 앱을 이미지로 만들어서 컨테이너로 생성한 후 접속 확인

- **작업 디렉토리를 생성하고 index.html 작성**
    ```html
    <html>
        <head>
            <title>Docker Image Build</title>
        </head>
        <body>
            <h1>nginx Web Server</h1>
        </body>
    </html>
    ```
- **Dockerfile 작성**
    ```dockerfile
    FROM nginx
    COPY index.html /usr/share/nginx/html
    ```

- **이미지 빌드**
    - `docker build -t mynginx .`
- **이미지 확인**
    - `docker images`
- **새로 만들어진 이미지를 가지고 컨테이너를 생성**
    - `docker run --name mynginx -d -p 8001:80 mynginx`
- **컨테이너 생성 여부를 확인하고 포트 개방 여부를 확인**

### 8) Dockerfile을 사용하는 이유

- **Ubuntu 에서 php 웹 페이지 연동**
    - 서버 직접 구축
        - apache 패키지 설치 및 확인
            ```bash
            sudo apt update
            sudo apt install -y apache2
            sudo service apache2 start
            sudo service apache2 status
            curl localhost:80
    
            # 기존 index.html 파일의 이름 변경
            sudo mv /var/www/html/index.html /var/www/html/index.html.org

            # index.html 생성 후 작성
            sudo apt install vim
            sudo vim /var/www/html/index.html

            # 테스트
            curl localhost:80

            # php 설치
            sudo apt -y install php

            # php 파일 생성
            sudo vim /var/www/html/index.php

            <?php
              phpinfo();
            ?>

            # apache2 서비스 재시작
            sudo service apache2 restart
            ```

    - 호스트 운영체제에 설치와 설정을 통해서 환경을 구성하고 개발 팀에서 제공하는 웹 소스를 이용해서 애플리케이션 서비스를 수행
    - 이런 방식은 인프라 구조가 커지면 인프라 구성 관리에 대한 부담도 커지게 됨
    - IaC를 이용하지 않고 직접 구성을 했기 때문에 동일한 구조를 만들려면 앞에서 설정한 내용을 그대로 다시 입력해야 한다. 
    - 예전에는 입력한 명령어를 텍스트 파일이나 스크립트 파일로 만들어서 텍스트 파일의 경우는 다시 입력을 했고 스크립트 파일은 스크립트 파일을 실행해서 작업이 이루어지도록 했다. 