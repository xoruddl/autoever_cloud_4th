# Kubernetes

## 3. Workload API

### 6) Job

- **개요**
    - 컨테이너를 사용해서 한 번만 실행하는 리소스
    - 여러 개의 작업을 병렬로 실행하면서 지정한 횟수의 컨테이너 실행을 **보장**하는 리소스
    - Run-to-Completion 이라고 부르기도 함.
    - ReplicaSet 과 다른 점은 정지되는 것을 전제로 만들어지는 것
    - ReplicaSet 은 정상 종료되는 횟수를 세지 못하기 때문에 배치 처리를 할 수 없음

- **파일을 2천자리까지 계산해주는 잡을 생성**
    - 매니페스트 파일: `sample-job.yaml`

        ```yaml
        apiVersion: batch/v1
        kind: Job
        metadata:
          name: pi
        spec:
          backoffLimit: 4
          template:
            spec:
              containers:
              - name: pi
                image: perl:5.34.0
                command: ["perl", "-Mbignum=bpi", "-wle", "print bpi(2000)"]
              restartPolicy: Never
        ```
    - 매니페스트 적용: `kubectl apply -f sample-job.yaml`
    - 잡을 확인: `kubectl get jobs`

        ```yaml
        apiVersion: batch/v1
        kind: Job
        metadata:
          name: hello-job
        spec:
          backoffLimit: 4
          template:
            spec:
              containers:
                - name: hello
                  image: busybox:1.36
                  command:
                    - sh
                    - -c
                    - |
                      echo "Job 시작"
                      sleep 10
                      echo "Job 완료"
              restartPolicy: Never
        ```
    - 매니페스트 적용: `kubectl apply -f sample-job.yaml`
    - 잡을 확인: `kubectl get jobs`
- **restartPolicy**
    - Job에서는 `spec.template.spec.restartPolicy` 에 `OnFailure` 이나 `Never` 중 하나를 지정해야 하는데 Never 를 지정하면 장애가 발생하면 새로운 파드를 만들고 OnFailure 지정하면 동일한 파드를 이용해서 잡을 다시 시작
    - 실패하는 JOB 을 만들어서 확인

        ```yaml
        apiVersion: batch/v1
        kind: Job
        metadata:
        name: errorjob
        spec:
        backoffLimit: 4
        template:
            spec:
            containers:
            - name: errorjob
                image: busybox:1.36
                command:
                - ls
                - unvalid path
            restartPolicy: Never
        ```

- **태스크와 작업 큐 병렬 실행**
    - 성공 횟수를 지정하는 `completions` 와 병렬성을 지정하는 `parallelism` 설정들의 기본값은 1이기 때문에 명시적으로 지정하지 않아도 동작은 변하지 않음
    - 2개의 파라미터는 잡을 병렬화하여 실행할 때 사용하는 옵션
    - `backoffLimit` 는 실패를 허용하는 횟수
    - 1회만 실행하는 태스크의 경우는 `completions` 와 `parallelism` 을 1로 설정하고 `backoffLimit` 은 0으로 설정하는 경우가 많다.
    - 여러 개를 병렬로 설정할 때는 parallelism 값을 조정하고 backoffLimit 를 설정하는 것을 권장
    - 여러 개의 잡을 순서대로 실행하고자 할 때는 completions 를 지정하지 않고 parallelism 을 1로 설정

- **일정 시간 후에 잡을 삭제**
    - `spec.ttlSecondsAfterFinished` 를 설정하면 된다.

        ```yaml
        apiVersion: batch/v1
        kind: Job
        metadata:
        name: samplejob
        spec:
        ttlSecondsAfterFinished: 30
        backoffLimit: 4
        template:
            spec:
            containers:
            - name: samplejob
                image: busybox:1.36
                command:
                - sh
                - -c
                - |
                    echo "Job 시작"
                    sleep 10
                    echo "Job 완료"
            restartPolicy: Never
        ```
    
- 매니페스트를 이용하지 않고 job을 생성: `kubectl create job my-job --image=busybox -- date`

### 7) CronJob

- 일정한 시간 간격을 가지고 작업을 수행하는 워크로드
- spec.schedule 에 스케쥴을 지정한다.
- 작업은 `spec.jobTemplate.spec.template` 에 지정
- 1분마다 명령을 수행하는 크론잡을 생성
    - 매니페스트: `sample-cronjob.yaml`

        ```yaml
        apiVersion: batch/v1
        kind: CronJob
        metadata:
        name: hello
        spec:
        schedule: "*/1 * * * *"
        jobTemplate:
            spec:
            template:
                spec:
                containers:
                - name: hello
                    image: busybox
                    imagePullPolicy: IfNotPresent
                    command:
                    - /bin/sh
                    - -c
                    - "date; echo Hello from the Kubernetes cluster"
                restartPolicy: OnFailure
        ```

- 크론잡을 일시정지: `kubectl patch cronjob hello -p '{"spec":{"suspend":true}}'`

- **동시 실행 제어**
    - `spec.concurrencyPolicy`에 설정
        - `Allow`: 기본값으로 동시 실행에 대한 제한을 하지 않음
        - `Forbid`: 이전 잡이 종료되지 않았을 경우 다음 잡은 실행되지 않음
        - `Replace`: 이전 잡을 취소하고 잡을 시작

- **일시적으로 작업이 밀리는 경우 어느 정도 시간 여유를 줄 것인지 설정 가능**
    - `spec.startingDeadlineSeconds` 에 시간을 설정
    - 매시간 작업을 수행하는 크론잡이 있을 떄 저 값을 300으로 설정하면 00분에 05분이 될 때까지만 작업을 수행
        - 5분이 넘으면 작업을 수행하지 않음
- **성공한 잡의 개수나 실패한 잡의 개수를 지정할 수 있음**
    - `spec.successfulJobsHistoryLimit` 와 `spec.failedJobsHistoryLimit` 에 설정
    - 성공과 실패한 잡의 로그를 확인할 수 있음

### 7) 노드 자원 보호

- **개요**
    - 근래에 몇 차례 문제가 발생한 노드에 파드를 할당하면 문제가 발생할 가능성이 높아짐
    - 이런 경우에는 특정 노드에는 더 이상 파드를 배치하지 않도록 설정 가능한데 그 기능이 cordon
    - cordon을 설정하면 더 이상 파드가 배치되지 않음
    - cordon 설정이 없으면 쿠버네티스는 노드에 파드를 가능한 균등하게 배치하려고 함
- **실습**
    - 메니페스트 생성: `sample-deployment.yaml`

        ```yaml
        apiVersion: apps/v1
        kind: Deployment
        metadata:
        name: sample-deployment
        spec:
        replicas: 2
        selector:
            matchLabels:
            app: sample-app
        template:
            metadata:
            labels:
                app: sample-app
            spec:
            containers:
            - name: nginx-container
                image: nginx
        ```
    - 배포: `kubectl apply -f sample-deployment.yaml`
    - 어떤 노드에 배치되어 있는지 확인: `kubectl get pods -o wide`
    - 파드 개수 늘리기: `kubectl scale deployment sample-deployment --replicas=4`
    - 어떤 노드에 배치되어 있는지 확인: `kubectl get pods -o wide`
        - k8s-node1 과 k8s-node2 에 균등하게 2개씩 배치
    - 파드 개수 복원: `kubectl scale deployment sample-deployment --replicas=2`
    - 어떤 노드에 배치되어 있는지 확인: `kubectl get pods -o wide`
        - k8s-node1 와 k8s-node2 에 균등하게 1개씩 배치
    - k8s-node1 의 상태 보존: `kubectl cordon k8s-node1`
    - 파드 개수 늘리기: `kubectl scale deployment sample-deployment --replicas=4`
    - 어떤 노드에 배치되어 있는지 확인: `kubectl get pods -o wide`
        - 새로운 파드는 worker2에만 배치됨
- **워커 노드의 모든 파드를 일시적으로 다른 곳으로 이동**
    - `kubectl drain k8s-node1 --ignore-daemonsets`
    - 해제: `kubectl uncordon k8s-node1`

## 4. Service API

- Service API 카테고리로 분류된 리소스는 클러스터 상의 컨테이너에 대한 EndPoint 를 제공하거나 레이블과 일치하는 컨테이너의 디스커버리

- **Service**
    - ClusterIP
    - ExternalIP
    - NodePort
    - LoadBalancer
    - Headless
    - ExternalName
    - None-Selector

- **Ingress**

### 1) 쿠버네티스에서의 통신

- 동일한 파드에 존재하는 컨테이너끼리는 localhost를 이용해서 통신이 가능
    - 컨테이너들은 포트를 구분해야 한다.
- 동일한 노드에 존재하는 다른 파드의 컨테이너와는 파드의 IP를 이용해서 통신이 가능?
- 쿠버네티스 클러스터의 내부 네트워크
    - 쿠버네티스 클러스터는 클러스터를 생성하면 노드 상에 파드를 위한 내부 네트워크가 자동으로 구성됨
    - 내부 네트워크의 구성은 사용하는 CNI 모듈 구현에 따라 다르지만 기본적으로 노드 별로 다른 네트워크 세그먼트를 구성하고 노드 간의 트래픽은 VXLAN이나 L2 Routing 등의 기술을 사용하여 전송함으로써 노드 간 통신이 가능하게 구성
    - 노드 별 네트워크 세그먼트는 쿠버네티스 전체에 할당된 네트워크 세그먼트를 자동으로 분할해 할당하므로 사용자가 설정하지 않아도 됨
    - 파드 간의 통신은 서비스를 사용하지 않고 IP를 가지고 가능하지만 서비스를 사용했을 때 얻을 수 있는 장점
        - 파드에 대한 트래픽 Loadbalancing
        - 서비스 디스커버리와 클러스터 내부 DNS
    - 디플로이먼트를 사용하면 여러 파드를 기동할 수 있는데 기동될 때마다 각각 다른 IP 주소를 할당받기 때문에 LoadBalancing 하는 구조를 자체적으로 구현하려면 각 파드의 IP 를 매번 조회하거나 전송 대상의 목적지를 설정해야 한다.
    - 서비스를 사용하면 여러 파드에 대한 LoadBalancing 을 자동으로 구성할 수 있으며 서비스를 EndPoint 를 제공
- **서비스를 활용하기**
    - 파드를 생성하는 메니페스트

        ```yaml
        apiVersion: apps/v1
        kind: Deployment
        metadata:
        name: sample-deployment
        spec:
        replicas: 3
        selector:
            matchLabels:
            app: sample-app
        template:
            metadata:
            labels:
                app: sample-app
            spec:
            containers:
            - name: nginx-container
              image: amsy810/echo-nginx:v2.0
        ```
    - 배포: `kubectl apply -f sample-deployment.yaml`
    - 파드의 IP 확인: `kubectl get pods -o wide`
    - 서비스 생성: `sample-clusterip.yaml`

        ```yaml
        apiVersion: v1
        kind: Service
        metadata:
          name: sample-clusterip
        spec:
          type: ClusterIP
          ports:
          - name: "http-port"
            protocol: "TCP"
            port: 8080
            targetPort: 80
          selector:
            app: sample-app
        ```
    - selector 에 연결하고자 하는 파드의 label 을 기재해주어야 한다.
    - 서비스 배포: `kubectl apply -f sample-clusterip.yaml`
    - 서비스 확인: `kubectl get services`
    - 서비스를 상세히 확인: `kubectl describe services sample-clusterip`
        - EndPoints 항목에 아무것도 보이지 않으면 selector를 잘못 지정했을 가능성이 높음
        - 로드밸런서 기능을 하는 ClusterIP가 만들어져서 ClusterIP로 요청을 보내면 EndPoint 로 요청을 전송해서 응답을 받아온다.
        - 여기서 이 애플리케이션의 자신의 IP 주소를 리턴하도록 만들어두어서 어떤 파드에서 요청을 처리했는지 알 수 있다. 
    - 서비스에 요청을 전송
        - `kubectl run --image=amsy810/tools:v2.0 --restart=Never --rm -i testpod --command -- curl -s http://10.99.35.217:8080`
        - 테스트를 위한 파드 생성: `kubectl run --image=amsy810/tools:v2.0 --restart=Never testpod`
        - 컨테이너 내부로 접속: `kubectl exec -it testpod -- /bin/bash`
        - http 요청: `curl -s http://클러스터IP:8080`
        - 여러 번 요청을 해서 로드밸런싱이 되고 있는지 확인

            ```bash
            root@testpod:/# curl -s http://10.99.35.217:8080
            Host=10.99.35.217  Path=/  From=sample-deployment-85d4455d66-khrv5  ClientIP=10.244.169.161  XFF=
            root@testpod:/# curl -s http://10.99.35.217:8080
            Host=10.99.35.217  Path=/  From=sample-deployment-85d4455d66-cwjtz  ClientIP=10.244.169.161  XFF=
            root@testpod:/# curl -s http://10.99.35.217:8080
            Host=10.99.35.217  Path=/  From=sample-deployment-85d4455d66-khrv5  ClientIP=10.244.169.161  XFF=
            root@testpod:/# curl -s http://10.99.35.217:8080
            Host=10.99.35.217  Path=/  From=sample-deployment-85d4455d66-cwjtz  ClientIP=10.244.169.161  XFF=
            root@testpod:/# curl -s http://10.99.35.217:8080
            Host=10.99.35.217  Path=/  From=sample-deployment-85d4455d66-d6nsp  ClientIP=10.244.169.161  XFF=
            root@testpod:/# curl -s http://10.99.35.217:8080
            Host=10.99.35.217  Path=/  From=sample-deployment-85d4455d66-khrv5  ClientIP=10.244.169.161  XFF=
            root@testpod:/# curl -s http://10.99.35.217:8080
            Host=10.99.35.217  Path=/  From=sample-deployment-85d4455d66-d6nsp  ClientIP=10.244.169.161  XFF=
            root@testpod:/# curl -s http://10.99.35.217:8080
            Host=10.99.35.217  Path=/  From=sample-deployment-85d4455d66-d6nsp  ClientIP=10.244.169.161  XFF=
            root@testpod:/# curl -s http://10.99.35.217:8080
            Host=10.99.35.217  Path=/  From=sample-deployment-85d4455d66-d6nsp  ClientIP=10.244.169.161  XFF=
            root@testpod:/# curl -s http://10.99.35.217:8080
            Host=10.99.35.217  Path=/  From=sample-deployment-85d4455d66-d6nsp  ClientIP=10.244.169.161  XFF=
            root@testpod:/#
            ```
- **하나의 서비스에 여러 포트를 할당하는 것도 가능**
    - http 와 https 의 경우 포트만 다를 뿐 동일한 서버에 접속하는 경우가 많은데 이 때 각각의 ClusterIP를 만들면 IP가 달라서 혼선이 올 수 있다.
    - 8080으로 오는 요청은 80번 포트로 연결하고 8443으로 오는 요청은 443번 포트로 연결하는 형태이다.
    - 현재 우리가 만든 파드가 443번이 Listen 상태가 아니라서 지금은 연결이 불가능
- **여러 포트를 할당하는 서비스: `sample-cluster-multi.yaml`**

```yaml
apiVersion: v1
kind: Service
metadata:
  name: sample-clusterip-multi
spec:
  type: ClusterIP
  ports:
  - name: "http-port"
    protocol: "TCP"
    port: 8080
    targetPort: 80
  - name: "https-port"
    protocol: "TCP"
    port: 8443
    targetPort: 443
  selector:
    app: sample-app
```

- **name을 이용한 파드 간 통신**
    - 파드를 두 개 가진 메니페스트: `sample-named-port-pods.yaml`

        ```yaml
        ---
        apiVersion: v1
        kind: Pod
        metadata:
        name: sample-named-port-pod-80
        labels:
            app: sample-app
        spec:
        containers:
        - name: nginx-container
            image: amsy810/echo-nginx:v2.0
            ports:
            - name: http
            containerPort: 80

        ---
        apiVersion: v1
        kind: Pod
        metadata:
        name: sample-named-port-pod-80
        labels:
            app: sample-app
        spec:
        containers:
        - name: nginx-container
            image: amsy810/echo-nginx:v2.0
            env:
            - name: NGINX_PORT
            value: "81"
            ports:
            - name: http
            containerPort: 81
        ```
    - 서비스 생성: `sample-named-port-service.yaml`

        ```yaml
        apiVersion: v1
        kind: Service
        metadata:
          name: sample-clusterip
        spec:
          type: ClusterIP
          ports:
          - name: http-port
            protocol: TCP
            port: 8080
            targetPort: http
          selector:
            app: sample-app
        ```

    - 배포
        - `kubectl apply -f sample-named-port-pods.yaml`
        - `kubectl apply -f sample-named-port-service.yaml`

- **클러스 내부 DNS와 서비스 디스커버리**
    - 쿠버네티스에서는 서비스 디스커버리를 `Service`가 제공
    - 서비스 디스커버리는 특정 조건의 대상이 되는 멤버를 보여주거나 이름에서 EndPoint 를 판별하는 기능
    - 쿠버네티스에서 서비스 디스커버리는 서비스에 속해 있는 파드를 보여주거나 서비스 명에서 EndPoint 정보를 반환하는 것
    - 서비스 디스커버리 방법
        - 환경 변수를 이용한 서비스 디스커버리
        - DNS A 레코드를 사용한 서비스 디스커버리
- **환경변수를 사용한 서비스 디스커버리**
    - 파드 내부에서는 환경 변수에서도 같은 네임스페이스 서비스를 확인할 수 있는데 -이 포함된 서비스명은 _로 변경한 후 대문자로 변환
    - 환경 변수에 등록된 서비스 정보 확인: `kubectl exec -it 파드명 -- env | grep -i kubernetes`
        - `kubectl exec -it sample-deployment-85d4455d66-cwjtz -- env | grep -i kubernetes`

- **DNS A 레코드를 사용한 서비스 디스커버리**
    - IP 주소 대신에 자동 등록된 DNS 레코드를 사용할 수 있음
    - 쿠버네티스에서 IP를 편하게 관리하려면 기본적으로 자동 할당된 IP 주소에 연결된 DNS 명을 사용하는 것이 바람직
    - FQDN
        - `서비스명.네임스페이스명.svc.cluster.local`
        - 확인: `dig sample-clusterip.default.svc.cluster.local`
    - 동일한 네임스페이스에서는 서비스 이름만으로 접근이 가능하고 다른 네임스페이스이면 이 경우에는 네임스페이스이름까지 적어야 함

### 2) ClusterIP Service

- **개요**
    - 쿠버네티스의 가장 기본이 되는 서비스
    - type:ClusterIp 서비스를 생성하면 쿠버네티스 내부에서만 통신 가능한 Internel Network 에 생성되는 가상 IP가 할당됨
    - ClusterIp 와 통신은 각 노드 상에서 실행 중인 시스템 구성 요소인 kube-proxy 가 파드로 전송

    - ClusterIP는 쿠버네티스 클러스터 외부에서 트래픽을 수신할 필요가 없는 환경에서 클러스터 내부 LoadBalancer 로 사용
    - 기본적으로 쿠버네티스 API에 접근하기 위해서 Kubernetes 서비스가 생성되어 있고 ClusterIP가 발급되어 있음
- **생성**

type:ClusterIP 를 설정

targetPort에 목적지 컨테이너 포트를 설정

port에는 외부에서 수신할 포트를 설정

- **가상 정적 IP 설정**
    - 애플리케이션과 데이터베이스를 배포하는 경우 데이터베이스 서버를 클러스터 내부 DNS 레코드를 사용해서 지정하는 것이 바람직
    - IP 주소로 하고자 하는 경우에는 spec.ClusterIP 란에 직접 고정 IP를 설정하는 것이 가능
    - 가상 정적 IP를 설정하는 서비스 메니페스트 생성: `sample-clusterip-vip.yaml`

        ```yaml
        apiVersion: v1
        kind: Service
        metadata:
          name: sample-clusterip-vip
        spec:
          type: ClusterIP
          clusterIP: 10.101.106.108
          ports:
          - name: "http-port"
            protocol: "TCP"
            port: 8080
            targetPort: 80
          selector:
            app: sample-app
        ```
    - 배포: `kubectl apply -f sample-clusterip-vip.yaml`

    - 도메인을 사용하지 않고 IP를 사용하는 이유
        - 개발 과정이어서 도메인이 없는 경우 테스트
        - DNS를 거치지 않아도 되기 때문에 속도가 빠름
        - 쿠버네티스에서는 IP를 사용하는 경우는 네트워크 연결을 확인하거나 장애를 분석할 떄 사용

- **ClusterIP는 한 번 생성되면 변경이 불가능**
    - 수정하고자 하면 삭제하고 다시 만들어야 한다.

### 3) ExternalIP Service

- **개요**
    - 특정 쿠버네티스 IP주소:포트번호 에서 수신한 트래픽을 컨테이너로 전송하는 형태로 외부와 통신할 수 있도록 하는 서비스
- **워커 노드의 IP를 확인**
    - `kubectl get nodes -o wide`
    - 192.168.56.7, 192.168.56.8

- **설정할 내용**
    - `spec.externalIPs`: 클러스터 노드 중 하나 이상으로 라우팅되는 외부 IP 주소를 지정한다. 이 IP의 할당·관리는 Kubernetes가 하지 않는다.
    - `spec.ports[].port`: Service가 노출하는 포트 번호이다. ClusterIP 및 `externalIPs`로 접근할 때 사용한다.
    - `spec.ports[].targetPort`: selector로 선택된 Pod 컨테이너의 실제 목적지 포트이다.
    - type은 ClusterIP 이다.
- **서비스 메니페스트 파일: `sample-externalip.yaml`**

```yaml
apiVersion: v1
kind: Service
metadata:
  name: sample-externalip
spec:
  type: ClusterIP
  externalIPs:
  - 192.168.56.7
  - 192.168.56.8
  ports:
  - name: "http-port"
    protocol: "TCP"
    port: 8080
    targetPort: 80
  selector:
    app: sample-app
```

- 노드의 컨테이너 안에서 테스트 해도 되지만 리눅스에서 curl -s http://노드의IP:8080 으로 접속이 가능

- 서비스 확인

### 4) NodePort Service

- **개요**
    - NodePort 서비스는 ExternalIP 서비스와 유사
    - ExternalIP는 지정한 쿠버네티스 노드의 IP주소:포트에서 수신한 트래픽을 컨테이너로 전송하는 형태로 외부와 통신을 하는데 NodePort는 모든 쿠버네티스 노드의 IP주소:포트에서 수신한 트래픽을 컨테이너에 전송하는 형태로 외부와 통신
    - ExternalIP 서비스에서 전체 노드의 트래픽을 수신할 수 있는 설정과 비슷하지만 Listen 할 떄 0.0.0.0:포트 를 사용해서 모든 IP 주소로 바인드하는 형태
- **NodePort 설정**
    - type:NodePort 로 설정
    - 별도로 IP를 설정하지 않음
    - nodePort를 설정하는데 이 포트는 30000~32767 범위 내여야 하며 설정하지 않으면 임의로 설정됨
- **서비스 작성: `sample-nodeport.yaml`**
```yaml
apiVersion: v1
kind: Service
metadata:
  name: sample-nodeport
spec:
  type: NodePort
  ports:
  - name: "http-port"
    protocol: "TCP"
    port: 8080
    targetPort: 80
    nodePort: 30080
  selector:
    app: sample-app
```
- 서비스 배포: `kubectl apply -f sample-nodeport.yaml`
- 서비스 확인: `kubectl get services`
- 서비스 확인: `curl -s 노드IP:30080`
    - 마스터 노드의 IP와 워커 노드 IP 아무것으로나 통신이 가능
- 노드의 IP 를 직접 사용하므로 IP 주소를 외부로 노출시켜야 한다.

### 5) LoadBalancer

- **개요**
    - 로드밸런서 서비스는 서비스 환경에서 클러스터 외부로부터 트래픽을 수신할 때 사용하는 서비스
    - 로드밸런서 서비스는 클러스터로 트래픽을 전달해주는 외부 LoadBalancer 와 함께 동작하며 레이블 셀렉터와 일치하는 파드로 트래픽을 전달하는 서비스
    - 외부 로드밸런서를 사용하려면 쿠버네티스 클러스터가 구축된 인프라가 이 구조에 맞도록 설계되어 있어야 한다. 
    - 현재는 Public Cloud 진영에서 로드밸런서를 제공하고 있고 직접 구축할 클러스터에서는 MetalLB를 이용해서 구현이 가능하다.
    - NodePort 나 ExternalIP는 결국 하나의 쿠버네티스 노드에 제공된 IP 주소로 통신을 하기 때문에 그 노드가 단일 장애점이 될 수 있지만 로드밸런서는 클러스터 외부에 만들어지기 때문에 노드에 장애가 발생해도 크게 문제가 없음
    - NodePort 를 생성하고 클러스터 외부의 로드밸런서에서 쿠버네티스 노드로 밸런싱을 하는 형태
    - 쿠버네티스 노드에 장애가 발생한 경우에는 그 노드에 트래픽을 전송하지 않음으로써 자동으로 복구하게 되어 있지만 LoadBalancer 가 노드 장애를 감지하고 목적지에서 제외 처리하기까지의 시간이 필요하므로 그동안 일시적으로 서비스 중단 현상이 발생

- **로드밸런서 메니페스트 생성: `sample-lb.yaml`**

```yaml
apiVersion: v1
kind: Service
metadata:
  name: sample-lb
spec:
  type: LoadBalancer
  ports:
  - name: "http-port"
    protocol: "TCP"
    port: 8080
    targetPort: 80
    nodePort: 30080
  selector:
    app: sample-app
```

- **배포를 해보면 External IP 가 계속 pending 상태**
    - 관리형 쿠버네티스가 아니라서 외부 IP가 설정되지 않아서 에러 상태가 된다.
    - 해결책은 metalLB 같은 도구를 이용해서 설정을 해야 한다.
